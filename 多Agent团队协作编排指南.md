# 多 Agent 项目协作团队编排指南

> **本文件是"完整走查版"**——给人读、也供 agent 在需要细节时按路径读。
> **执行版技能**在 [`engineering/multi-agent-squad/SKILL.md`](./engineering/multi-agent-squad/SKILL.md)
> 与 [`engineering/commercial-squad/SKILL.md`](./engineering/commercial-squad/SKILL.md)。

**两者的分工（别重复读）**：

| | 执行版技能 | 本指南 |
|---|---|---|
| 形态 | 纪律 + 模板 + 判据（agent 加载后照做） | 完整走查 + 实例 + 逐项映射 |
| 独有内容 | 三条铁律的硬约束、三段式模板、六类消息 | **商业侧角色表**（Validator/Pricer/Biller/Grower）、**DSH 机制逐项映射与陷阱**、交接协议细节、落地路线每环的验证方法 |
| 什么时候读本文件 | — | ① 建**商业项目**团队 ② 不确定某个 DSH 机制怎么用 ③ 要从 0 把团队搭起来 |

> 这是本体系的**多 agent 协作落地指南**，面向"一个人 + 一支 agent 团队"的场景。
> 体系里的工程技能本质是**给"一个 agent"的指令**，本身不构成编排；
> 但把**角色、阶段、交接、上下文预算**想得很清楚——这份指南就是把这些映射到 DSH 实际可用的多 agent 机制上。

> **配套**：可调用的技能版本是 [`engineering/multi-agent-squad/SKILL.md`](./engineering/multi-agent-squad/SKILL.md)（编排纪律）与 [`engineering/commercial-squad/SKILL.md`](./engineering/commercial-squad/SKILL.md)（商业流程）。
> 本文是**给人读的完整版**（含背景、推理与路线图）；技能是**给 agent 读的执行版**。两者内容对齐。

---

## 0. 先搞清一件事：这套体系里"多 agent"在哪

体系里已经埋了多 agent 的种子，只是没这么叫：

| 体系里已有的机制 | 它其实是多 agent 的什么 |
|---|---|
| **阶段（phase）与阶段边界**（`ask-matt/PHASE-BOUNDARIES.md`） | **什么时候该换一个 agent**：阶段边界 = agent 的出生/死亡点 |
| **一手来源 vs 二手来源** | **为什么交接必须落文件**：`/handoff` 产出的文件就是 agent 之间的消息 |
| **`/research` 后台 agent** | 一个真正的 AFK 子 agent |
| **`/code-review` 的"两个并行子 agent"** | 一个双 agent 并行审查拓扑 |
| **`/grilling`：事实归 agent、决策归人** | agent 分工的基本协议：查证的活别推给人，判断的活别替人做 |
| **`/wayfinder` 的决策工单地图** | 团队共享的**任务队列/黑板** |
| **`/to-tickets` 的阻塞边（blocking edges）** | agent 之间的**依赖 DAG** |

**一句话**：本体系教你怎么**管理一个 agent 的上下文和阶段**；多 agent 就是把这套纪律用在**多个上下文**上，并用**文件**把它们连起来。

### 0.1 多 agent 到底买到了什么（先想清楚，再决定要不要用）

多 agent **不是"更多算力"**。它的成本很真实：**共享内存没了**（子 agent 看不见你的对话，也看不见彼此）。

它买到的是两样东西：

| 买到 | 含义 | 不可替代的原因 |
|---|---|---|
| **独立性** | 审查者不被实现理由污染 | 一个 agent 无法客观审查自己写的东西——它记得自己为什么那么写 |
| **并行** | 互不阻塞的活同时干 | 单窗口只能串行 |

**再加一样**：**上下文预算的扩展**。单个窗口的智能区是**上下文窗口的一部分**（经验值 ≈ 15%），超过就**无声降智**；多窗口等于把这个上限变成可扩展的。

**成本**：每次交接都要**写文件 + 新窗口重新建立上下文**。所以**只有在能买到独立性或并行时才值得**——纯为了"快"而拆，通常更慢。

---

## 1. 三条铁律（绝不要违反）

### 铁律 A：文件是唯一的共享内存

不同 agent 有各自独立的上下文窗口，**它们看不见彼此的对话**。
所以体系里那些"落文件"的设计（`CONTEXT.md`、ADR、spec、工单、handoff 文档）在多 agent 下**从"好习惯"升级为"唯一可行"**。

> **禁止**：靠"我记得上一轮讨论过 X"来协作。
> **必须**：每一条需要跨 agent 传递的信息，都写进文件，并给一个稳定的路径。

**补充要求**：交接文件里必须有**推理**，不只是结论。结论可以猜，推理不能。

### 铁律 B：一个 agent 一个窗口，不要跨阶段复用

`PHASE-BOUNDARIES.md` 的决策树在多 agent 下就是"要不要新开一个 agent"：

- 下一阶段需要当前阶段作**一手来源**（如 逼问 → 实现）→ **保留同一个 agent**（或不交接，直接续）
- 当前上下文对下一步**完全无关** → **回收**，开新 agent
- 要换目录/换平台/交给别人 → **写文件交接**
- 任务能**离线跑**、范围紧 → **派子 agent**
- 以上都不是（上下文相关、同环境、你需要盯）→ **压缩后继续**

**最常犯的错**：让一个 agent 一路从逼问干到实现干到审查。到审查那一步它已经**无法客观**，且上下文被污染。

### 铁律 C：审查者必须独立，且与实现者不同窗口

`code-review` 把审查拆成**两条轴**（标准 + 规格），并且明确要求**并行子 agent、互不污染**。
在多 agent 团队里这条更硬：

> **同一个 agent 不能既实现又审查自己**——它的上下文里已经有"我为什么这么写"的全部理由，这恰恰是审查要排除的东西。

**验收这条铁律的唯一方法**：Reviewer 是不是**真的**发现了 Builder 没注意的问题？
如果每次都只会说"看起来不错"，说明独立性已经失效——去查它是不是读了实现者的理由。

---

## 2. 角色映射表：技能 → 团队角色

用体系里的技能拼出一支最小可用团队。**每个角色本质上是"一组技能的加载器 + 一条职责边界"**。

| 角色 | 加载哪些技能 | 输入（从文件读） | 输出（写文件） | 备注 |
|---|---|---|---|---|
| **Navigator / Lead** 领航员 | `grilling` + `domain-modeling` + `to-spec` + `to-tickets` + `ask-matt` | 人的模糊想法 | `CONTEXT.md`、`docs/adr/*`、`spec.md`、`.scratch/<feature>/issues/*.md` | 团队的大脑；**必须与人实时交互**（HITL）；**唯一常驻** |
| **Wayfinder** 探路人 | `wayfinder` + `grilling` + `research` | 一大团雾（超出单会话的工程） | `map.md` + 决策工单 + **决策**（不是交付物） | 只在"路还看不见"时启用；不写代码 |
| **Researcher** 研究员 | `research` | 一个具体问题 | 带引用的 markdown 笔记 | **纯 AFK**，可并行多个 |
| **Architect** 架构师 | `codebase-design` + `improve-codebase-architecture` + `domain-modeling` | 现有代码 + 术语表 | 加深机会报告（HTML）、接口设计、ADR | 与 Navigator 有重叠，小团队可合并 |
| **Builder** 实现者 | `implement` + `tdd` + `fullstack-delivery` | 一张工单（`.scratch/.../issues/NN-*.md`） | 代码 + 测试 + commit | **一张工单一个 builder 窗口** |
| **Reviewer** 审查者 | `code-review` | diff + 规格 + 仓库规范 | 双轴报告（标准 / 规格） | **两个并行子 agent**，或两个独立 reviewer |
| **Hardener** 加固者 | `production-readiness` | 可运行的系统 | 逐项闭环的加固报告 | **上线前必入场**（新增） |
| **Debugger** 诊断者 | `diagnosing-bugs` | 一个症状 | 反馈回路脚本 + 回归测试 + 根因 | 只在有 bug 时入场 |
| **Shipper** 发布者 | `launch-and-ops` | 冻结的构建产物 | 上线 + 回滚预案 + runbook | 上线时入场（新增） |
| **Triage** 分诊员 | `triage` | 外部进来的 issue / PR | 分诊标签 + agent 简报 | 处理**不是你创建的**东西 |
| **Wizard** 向导作者 | `wizard` | 需要人操作的流程 | 交互式 bash 脚本 | 只在"只有人能做的步骤"时用 |

### 2.1 商业侧角色（本目录新增）

| 角色 | 加载哪些技能 | 输入 | 输出 | 何时入场 |
|---|---|---|---|---|
| **Validator** 验证者 | `product-validation` | 一个想法 | 证据清单 + 继续/转向/放弃 | 最早；**不许读代码、不许写代码** |
| **Pricer** 定价者 | `pricing-and-monetization` | 已验证的问题 + 人群 | 定价方案 + 单位经济账 | G2 之前；**不许读代码** |
| **Biller** 计费实现者 | `payment-and-billing` | 定价方案 | 收款链路（测试模式验证过） | 要收钱时 |
| **Grower** 增长者 | `growth-and-analytics` | 上线的产品 | 埋点 + 漏斗 + 留存结论 | 有真实用户后 |

> **Validator / Pricer 不许读代码仓库**是一条硬约束：一旦它们看见代码，就会开始评论实现，
> 而它们要回答的是**市场问题**——这是"用代码逃避验证"的 agent 版本。

**关键设计**：这些角色大多数**不常驻**。它们是**被需要时实例化的窗口**——用完就回收。常驻的只有 **Lead**。

---

## 3. 主流水线：一次功能从想法到合并

把原主流程展开成**多 agent 的泳道图**（→ 表示交接发生）：

```
                ┌──────────────────────────────────────────────┐
   [人的想法] → │ ① Align 对齐                                 │
                │   Navigator 跑 grilling（+domain-modeling）   │
                │   产出：CONTEXT.md 术语、ADR                 │
                └──────────────────┬───────────────────────────┘
                                   │ 交接：同一窗口继续（一手来源是需要的）
                ┌──────────────────▼───────────────────────────┐
                │ ② Spec & Tickets 定规格拆工单                │
                │   Navigator: to-spec → to-tickets            │
                │   产出：spec.md + 带阻塞边的工单文件          │
                └──────────────────┬───────────────────────────┘
                                   │ 交接：窗口可以在这里回收
                                   │ （决策已经沉淀进文件）
        ┌──────────────────────────▼───────────────────────────┐
        │ ③ Build 施工（按工单前沿，可并行）                    │
        │   每张工单 → 一个新 Builder 窗口                      │
        │   内部：implement → tdd（一次一个红绿垂直切片）        │
        │   产出：代码 + 测试 + commit                          │
        └──────────────────────────┬───────────────────────────┘
                                   │ 交接：写文件（改了哪些文件、跑了什么）
        ┌──────────────────────────▼───────────────────────────┐
        │ ④ Review 审查（必须独立）                            │
        │   两个并行 Reviewer：标准轴 / 规格轴                  │
        │   产出：双轴报告；不合并、不重排                      │
        └──────────────────────────┬───────────────────────────┘
                                   │ 发现问题 → 回 ③（新的 Builder 窗口）
                                   │ 通过 → 合并
        ┌──────────────────────────▼───────────────────────────┐
        │ ⑤ Harden 加固（上线闸门）                            │
        │   Hardener: production-readiness 七节逐项闭环         │
        └──────────────────────────┬───────────────────────────┘
        ┌──────────────────────────▼───────────────────────────┐
        │ ⑥ Ship 发布                                          │
        │   Shipper: launch-and-ops 上线 + 回滚预案 + runbook   │
        └──────────────────────────────────────────────────────┘
```

**旁路（不一定走主干）**：

- **Bug 来了** → 直接进 `Debugger`（`diagnosing-bugs`），它修完后如果发现"根本没有好接缝"，把结论转给 `Architect`。
- **说不清的设计问题** → `prototype` 做一次性原型，答案折回 ①。
- **需要外部事实** → `Researcher` 并行跑，结论带进 ①。
- **一大团雾的工程** → 先走 `wayfinder` 出决策地图，再在 ② 收敛。

**每一步的交接方式，按 `PHASE-BOUNDARIES.md` 的五选一来决定**——这是这套方法论最实用的一块。

---

## 4. 落到具体机制：DeepSeek Harness（DSH）里怎么搭

> 不同平台的多 agent 机制名字不同（Claude Code 是 subagent / plugin；CodeBuddy 是 Team + Task + send_message）。
> 下面是**通用形状**，括号里是 **DSH 的对应物**。

### 4.0 把角色映射到 DSH 的机制

| 你想干的事 | DSH 机制 | 注意 |
|---|---|---|
| 派一个**离线、范围紧**的活 | 后台子 agent | 默认后台跑；**派完继续干自己的活，不要空等** |
| 需要**结果才能继续** | 前台子 agent | 只在下一步确实依赖它时用（`run_in_background: false`） |
| **批量多阶段扇出**（审计 N 个文件、迁移、多角度验证） | 工作流脚本 | 编排写成脚本（fan-out / pipeline / parallel），不要一轮轮手工派 |
| 给**已存在**的 agent 追加活 | 发消息（续同一会话） | 它**当前轮结束前不会被打断**——不能改已在做的事 |
| 需要**继承当前上下文**的子 agent | 派生型子 agent | 子 agent 拿到本会话**已完成**的轮次（不含进行中的这一轮） |
| 停掉跑偏的 agent | 中断 | 只停当前轮；**它自己派的下级不会跟着停** |
| 长任务、后台进程 | 后台作业 | 有 id，可读输出、可终止 |

**并行纪律**：互不依赖的活**在同一条消息里一起派**，然后继续做自己的事。这是多 agent 唯一的真收益来源。

**成本纪律**：每个子 agent 都要**重建上下文**。所以派活时必须给**文件路径**，而不是让它自己去探索仓库——
否则它会把大量 token 花在"重新发现"，而且发现的还是过时结论。

### 4.1 建团队（可选，管理用）

多 agent 协作不需要"建团队"这个动作本身——它只是给成员一个共享目录和通信邮箱。
如果只是**一次性派活**，直接用子 agent 就行；如果要**多个成员持续互相通气**（比如 Builder 想问 Reviewer 问题），才建团队。

### 4.2 派活：把角色实例化成窗口

**给每个成员的三段式 prompt（这是全部诀窍）**：

```
【身份】你是 <角色名>。这次任务你只做 <职责边界>，越界的事停下来问 lead。

【技能】先读 <技能文件绝对路径>，严格按它执行。它定义的阶段、完成标准、
       反模式都是硬约束。

【输入/产出】
  输入：<文件路径列表——这是你唯一的信息来源，不要去猜别的>
  产出：<必须写出的文件路径 + 完成标准>
```

> **为什么必须写死 SKILL.md 路径**：`model-invoked` 技能靠 description 自动触发，
> 但**跨 agent 时不要指望它**——子 agent 有自己的系统提示和独立上下文，
> 它不知道你这边加载了什么。**在 prompt 里点名路径，是唯一可靠的技能传递方式。**

**示例 —— Builder 的 prompt**：

```
【身份】你是 Builder。只负责实现工单 03，不修改其他文件，不做架构决策。
   遇到工单描述不清楚，停下来向 lead 报告，不要自己臆断。

【技能】先读 <库根>\engineering/implement/SKILL.md，
   并在施工中读 <库根>\engineering/tdd/SKILL.md。
   遵守：红在绿前、一次一个垂直切片、只在预先约定的接缝上测试。
   重构不属于红绿循环，留给评审。

【输入/产出】
  输入：.scratch/<feature>/issues/03-<slug>.md（唯一需求来源）
        CONTEXT.md（术语，命名必须一致）
  产出：代码 + 测试 + commit；完成后把「改了哪些文件 / 跑了哪些命令 / 结果」
        写进 .scratch/<feature>/issues/03-<slug>.md 的 ## Comments
  完成标准：工单里每条验收标准都能勾上，且测试套件全绿。
```

**示例 —— Reviewer 的 prompt**：

```
【身份】你是 Reviewer，只审查，不改代码。你没有参与实现，也不要为它辩护。

【技能】读 <库根>\engineering/code-review/SKILL.md。
   沿两条轴：
     标准轴 = 仓库成文规范 + Fowler 坏味道基线（skill 里的 12 条，全文照搬）
     规格轴 = 工单/规格是否被忠实实现
   两轴必须分开汇报，不要合并、不要跨轴挑冠军。

【输入】git diff <fixed-point>...HEAD；spec；工单文件
【产出】两份报告（各 <400 字）+ 每轴最严重问题
```

**示例 —— Hardener 的 prompt**：

```
【身份】你是 Hardener。任务是让这个系统**能上线**，不是让它更好看。
   只做 production-readiness 清单里的事，不加新功能。

【技能】读 <库根>\engineering/production-readiness/SKILL.md

【输入】<项目根>；.env.example；<部署方式说明>
【产出】逐项闭环报告（现状证据 / 改动 / 验证方式 / 未解决风险）。
   没验证过的不许标"已完成"。
```

### 4.3 通信协议

成员之间不要"聊天"，要**发结构化消息**。约定这六类：

| 消息类型 | 用途 | 要求 |
|---|---|---|
| **HANDOFF** | 交接工作 | 必须是文件路径，不是对话摘要 |
| **QUESTION** | 需要别人知道的事实 | 指明问题、截止、期望产出 |
| **BLOCKED** | 卡住了 | 说清卡在哪、试过什么、需要谁做什么 |
| **REVIEW** | 请求审查 | 附 diff 固定点 + 规格路径 |
| **DONE** | 完成 | 附产出文件路径 + 验收标准的逐条核对 |
| **DECISION** | 记录不可逆决策 | 写进 `docs/adr/`，不能只留在对话里（新增） |

**反模式**：context 里裸聊"你看下这个""好的我改下"。成员上下文是隔离的，**没写进文件/消息的等于没发生**。

### 4.4 审批与闸门

"阶段边界"在多 agent 下要加一道**人工闸门**——尤其这几个时刻：

1. **工单拆分完成时**（`to-tickets` 第 4 步"拷问用户"）：粒度、阻塞边要人确认。
2. **验收标准确认时**：Builder 说"做完了"之前，要能逐条对照工单里的 checklist。
3. **任何不可逆动作**：删数据、改 schema、发布、付款配置、对外接口变更。
4. **商业决策**：G1/G2 判定、定价、花钱的动作——见 `commercial-squad`。

这对应工作区 `AGENTS.md` 的铁律"改动必须经过用户确认"——**多 agent 放大了改动速度，所以闸门更要前置**。

---

## 5. 文件即接口：团队的共享"数据库"

多 agent 团队的目录约定（本地 markdown 追踪器，零外部依赖）：

```
项目根/
├── CONTEXT.md                  ← 统一语言（所有 agent 命名依据）
├── CONTEXT-MAP.md              ← 多上下文时才需要
├── docs/
│   ├── adr/0001-*.md           ← 难以逆转的决策（含"触发重新评估的条件"）
│   └── agents/
│       ├── issue-tracker.md    ← 追踪器约定（本地/GitHub/…）
│       ├── triage-labels.md    ← 分诊标签映射
│       └── domain.md           ← 领域文档消费规则
├── .scratch/
│   ├── validation/             ← product-validation 产出（证据清单）
│   ├── pricing/                ← pricing-and-monetization 产出（单位经济账）
│   ├── tech/                   ← tech-selection 产出（约束 + 成本模型）
│   └── <feature>/
│       ├── spec.md             ← to-spec 产出
│       └── issues/
│           ├── 01-<slug>.md    ← 一张工单一个文件，带 Blocked by
│           ├── 02-<slug>.md
│           └── 03-<slug>.md
└── .out-of-scope/*.md          ← 被否决的请求（防止重复讨论）
```

**为什么这些文件就是"接口"**：
它们让**新开的 agent 窗口**能在 30 秒内获得全部必要上下文，而不需要读一遍别人的对话历史。这是多 agent 协作能成立的**唯一**基础。

---

## 6. 落地路线：一小环一小环地搭（强烈建议）

按工作区 `AGENTS.md` 的铁律——**一次只验证一个环节**。别一上来就上 7 个 agent。

### 第 0 环：单 agent + 文件纪律

先只做**一件事**：把 `CONTEXT.md` 这份术语表建起来，并在每次任务里让 agent 读它。
验证：命名是否开始一致、同一件事是否不再反复解释。
**这一步不通过，后面全都白搭**——因为共享语言是多 agent 的公共接口。

### 第 1 环：2 个 agent（Builder + Reviewer）

只加审查者。做一次小改动：
Builder 实现 → **新开窗口**的 Reviewer 审查 → 回改。
验证：Reviewer 是否**真的**发现了 Builder 没注意的问题（如果总是"看起来不错"，说明审查看的是同一批信息，没起到独立作用）。

### 第 2 环：加 Navigator（3 个 agent）

加入"逼问 + 拆工单"。做一次中等功能：
Navigator 逼问 → 写 spec → 拆 3 张工单 → 每张工单派新 Builder → 各自 Reviewer。
验证：Builder 是否能**只看工单文件**就干完活（不需要问 Navigator）。
**如果 Builder 老是要回头问，说明工单写得不够自足**——这是 `to-tickets` 的质量问题，回去修工单。

### 第 3 环：加并行

同一个 frontier 上多张**互不阻塞**的工单，**同时**派多个 Builder。
验证：文件不冲突、阻塞边真的起作用。
（这一步要非常小心——见下一节坑位。）

### 第 4 环：按需加角色

- 有难 bug → `Debugger`
- 要大范围架构改进 → `Architect` + `improve-codebase-architecture`
- 要上线 → `Hardener`（`production-readiness`）+ `Shipper`（`launch-and-ops`）
- 有外部 issue 涌入 → `Triage`
- 有只有人能做的步骤 → `Wizard`
- 一大团雾 → `Wayfinder`
- **要做商业项目** → 从 `commercial-squad` 的四道闸门开始

### 第 5 环：反馈闭环

定期（如每隔几天）跑一次 `improve-codebase-architecture` 的"体检"，把结果变成下一轮的工作。
原作者的定位很准：**是体检，不是抢救**。

---

## 7. 常见坑（多 agent 特供）

1. **两个 agent 同时改同一个文件。**
   靠 `to-tickets` 的**阻塞边**在纸面上杜绝；靠工单里的"哪些文件归你"作为运行时护栏。冲突了别硬合——用 `resolving-merge-conflicts` 逐块按意图裁决。
   **特别注意**：数据库迁移、路由注册表、依赖清单这类共享文件必须**串行**改。

2. **审查者被"污染"。**
   让实现者"顺便审一下"是最常见的自欺。**审查必须新窗口、新上下文**，而且最好两轴分开跑。

3. **交接靠对话摘要。**
   `/compact` 和"你总结一下给下一个 agent"都会**丢 why**——那是**有损的、单向的**。交接要落**文件**，且文件里要有**推理**，不只是结论。

4. **工单不自足。**
   衡量标准非常明确：**Builder 只看工单能不能干完**。干不完就是工单的缺陷，不是 Builder 的。用 `AGENT-BRIEF.md` 的原则自查——行为式、不引文件路径/行号、有可独立验证的验收标准、有明确的范围外。

5. **让 agent 替人做决策。**
   `grilling` 的规则：**事实是 agent 的活，决策是人的**。多 agent 下更要注意——别让"系统设计者"顺手替你定了产品方向。

6. **一股脑派一堆 agent 并行。**
   垂直切片是"一次一个"的，多 agent 也是。**先串行跑通，再逐段并行**——这是第 6 节的路线图。

7. **无视上下文预算。**
   每个 agent 的正常工作区间是上下文窗口的**一部分**（经验值 ≈ 15%，"智能区"）。一个窗口干太多事会**降智**，而且降智是无声的。**在阶段边界处换窗口**，别硬撑。

8. **技能触发不确定。**
   `model-invoked` 技能靠 description 自动触发，但**跨 agent 时不要指望它**——**在 prompt 里显式点名要读哪个 SKILL.md**，最可靠。

9. **子 agent 跑偏很久才发现。**（新增）
   派活时就把**范围和完成标准**写死；只有"范围紧、能离线"的活才适合派出去。跑偏了用中断停它，**但记住它自己派的下级不会跟着停**。

10. **每个 agent 重复做同样的调研/读同样的文件。**（新增）
    这是纯粹的浪费，而且是多 agent 最容易发生的隐性成本。**把结论沉淀成文件**（调研笔记、`CONTEXT.md`、ADR），让下一个 agent 读**结论**，而不是重跑**过程**。

11. **"派完就空等"。**（新增）
    派了后台子 agent 之后阻塞等它，等于把并行退化成串行。**派完继续干互不依赖的活**，需要结果时再收。

---

## 8. 平台适配说明（DSH）

下面这些机制在不同 harness 里叫法不同，**在本环境下需要替换或省略**：

| 通用机制 | 在 DSH 里怎么落地 |
|---|---|
| `/clear`、`/compact` | Claude Code 的会话命令。本地对应：**开新窗口**（=clear）、**写交接文件后开新窗口**（=compact 的更可靠版本） |
| `/handoff` 存到 OS 临时目录 | 多 agent 团队建议改成**存进仓库**（如 `.scratch/<feature>/handoff-*.md`），否则别的 agent 找不到 |
| `gh` / `glab` CLI | issue 追踪器相关技能依赖它们；本地团队直接用**本地 markdown 追踪器**（`issue-tracker-local.md`）即可，零依赖 |
| Plugin / marketplace 安装 | 已省略——本目录就是**可读可改的副本** |
| `agents/openai.yaml` | **已清理**（原为 Codex 平台配置，DSH 不读、零引用，属死重） |
| **调用策略字段** | **DSH 用 `disable-model-invocation`**（连字符、值须能被解析为布尔）。`user-invocable: true` 是空操作。详见 [`DSH-INTEGRATION.md`](./DSH-INTEGRATION.md) |
| **技能发现深度** | **新增差异**：DSH **只发现一层**（`<root>/<name>/SKILL.md`），本目录的 `engineering/xxx/` 结构**不能**直接当技能根用；要么按路径读取（推荐），要么打平安装 |
| 术语 | 术语表保留中英对照；技能名（`tdd`、`code-review` 等）保持英文以便调用与检索 |

---

## 9. 最小可运行清单（拿去就能用）

1. **放技能**：把本目录（或其中的 `engineering/`、`productivity/`）当作技能的**只读参考库**；要能被自动触发就按 [`DSH-INTEGRATION.md`](./DSH-INTEGRATION.md) 打平安装。
2. **建仓库基础**：在你的项目里建 `CONTEXT.md`（术语表）+ `docs/adr/`。
3. **建追踪器**：建 `.scratch/` 目录，按 `issue-tracker-local.md` 的约定。
4. **写角色 prompt**：用第 4.2 节的三段式模板，复制粘贴改路径即可。
5. **定消息协议**：用第 4.3 节的 6 类消息名。
6. **按第 6 节的路线图推进**：第 0 环 → 第 1 环 → ……**每一步都在真的用起来之后才进下一步**。
7. **定期体检**：每隔几天跑一次 `improve-codebase-architecture` 的流程。
8. **做商业项目**：从 [`commercial-squad`](./engineering/commercial-squad/SKILL.md) 的 **G1 验证**开始——**不通过不写代码**。

---

## 附：一页速查

```
想法
 ├── 有雾、超出单会话？ → wayfinder（决策地图，不写代码）
 ├── 需要外部事实？     → research（并行 AFK）
 ├── 设计问题说不清？   → prototype（一次性，答案折回）
 ├── 商业项目没验证？   → product-validation（先过 G1，别写代码）
 └── 正常路径
      grilling（事实 agent 查，决策你来定）
        → CONTEXT.md / ADR（就地更新）
        → to-spec（不访谈，只综合）
        → to-tickets（垂直切片 + 阻塞边 + 拷问用户）
        → 每张工单一个 Builder：implement → tdd（红绿切片）
        → 两个并行 Reviewer：标准轴 / 规格轴
        → 通过则合并；不通过则回炉（新窗口）
        → Hardener: production-readiness（上线闸门）
        → Shipper: launch-and-ops（上线 + 回滚 + runbook）

坏了？→ diagnosing-bugs（先建能变红的回路，再谈假设）
乱了？→ improve-codebase-architecture（体检，不是抢救）
外部来的？→ triage（不是你创建的才算）
只有人能做的？→ wizard
收钱？→ payment-and-billing        涨不动？→ growth-and-analytics
```

**记住三条**：
1. **文件是唯一共享内存。**
2. **阶段边界换窗口，别让一个 agent 干到底。**
3. **审查者必须独立于实现者。**
