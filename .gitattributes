# 统一换行符：**全部文本文件用 LF**（存储与检出都一致）。
#
# 为什么写死：
#   1. 本仓库混合了 LF（自研文件）与 CRLF（早期继承的翻译件），同一个"改一行"的提交
#      会因为换行符被算成"整个文件重写"，diff 完全没法读 —— 而 diff 是本库唯一的评审手段。
#   2. git 钩子必须 LF：CRLF 会让 sh 报 `\r': command not found`，**钩子静默失效**（比报错更糟）。
#   3. Linux/macOS 上的 `.sh` 模板同理，CRLF 会让它们直接跑不起来。
#
# 注意：本文件生效后 git 会按 LF 归一化存储。若工作区文件是 CRLF，会先显示为已修改，
# 这是预期行为（归一化），不是内容变化。
* text=auto eol=lf
