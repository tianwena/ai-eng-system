# 速查卡

> 本文件是各技能的**压缩速查**，由 `scripts/extract-cheatsheets.mjs` 从技能正文末尾抽出。
> 
> **为什么单独放这里**：速查对**人**有用，但对**已读正文的 agent** 是重复内容。
> 放在文档层可以同时满足两边——agent 加载的 `SKILL.md` 变精简，速查仍然随手可查。
> 
> 收录速查的技能：`commercial-squad`、`fullstack-delivery`、`growth-and-analytics`、`launch-and-ops`、`multi-agent-squad`、`payment-and-billing`、`pricing-and-monetization`、`product-validation`、`production-readiness`、`tech-selection`

---

## commercial-squad


```
四道闸门（每道都要证据，不是感觉）
  G1 有人要  → ≥5个真实的人具体描述问题（不许用代码回答）
  G2 能收钱  → 有人真的愿意付（不是"会考虑"）
  G3 能交付  → 端到端跑通（真数据 + 真部署）
  G4 能重复  → 第二次交付成本可接受

顺序
  Validate → Price → Select → Build → Harden → Bill → Ship → Grow

一个人做完的唯一方法
  砍用户群 → 砍平台 → 砍功能 → 砍自动化 → 砍自研 →（质量不许砍）

铁规
  1. G1 没过，不写产品代码
  2. 商业决策由人做，agent 只给证据和选项
  3. 上线前必跑 production-readiness
  4. 埋点在上线前就位
  5. 不可逆决策落 ADR

工程流程问题 → ask-matt
商业流程问题 → 本技能
```

---

## fullstack-delivery


```
第一刀：最薄的垂直切片
  页面 → 端点 → 表 → 写 → 读 → 显示（跑通再长）

依赖顺序
  切片 → 数据模型/迁移 → 认证 → 授权 → API契约/校验
       → 前端四态 → 后台任务 → 外部集成 → 加固 → 上线

每个切片的五样交付物
  能跑 / 有测试 / 有错误处理 / 可观测 / 部署通过

数据层铁律
  迁移可回滚或显式标注；每表能答"归谁"；约束进数据库
  UTC 存时间；钱用整数；软删除先决定再写

前端四态
  加载中 / 空 / 错误 / 成功 —— 缺一不可

外部集成统一套
  超时 + 重试(幂等) + 降级(显式) + 日志 + 告警 + 进出都校验

多 agent 并行安全
  契约先行 / 迁移独占 / 工单写明文件归属 / 共享文件串行
```

---

## growth-and-analytics


```
北极星：一个能代表"用户真的获得价值"的数字
    分阶段：早期看验证性指标 → 有几十用户后看留存+time-to-value → 走平后才收成一个
  判据：反映价值 / 可影响 / 领先收入
  加上：留存率（最重要）、付费转化、CAC 与回本周期

埋点（上线前就位）
  命名 <对象>_<动作>；必带 user_id/时间/来源/版本
  必埋：到达 / 注册开始+完成 / 核心动作 / 门槛事件
        / 付费漏斗 / 错误与放弃 / 流失信号
  关键动作埋"开始"和"完成"两点

漏斗：看相邻两段转化率，改绝对值最低的那段
  最重要：time-to-value（首次到达核心价值的时间）越短越好

留存：队列留存，看曲线走平 + 新队列是否变好
  流失：找"退化的起点"（不是最后动作）+ 问"那一天" + 分群对比

渠道：有效 CAC = 花费 ÷ 该渠道带来的【留存用户数】
  只投：有效 CAC < 生命周期毛利

每轮产出 ≤3 项动作，每项带假设和验证方式
  并明确写出【不做什么】
```

---

## launch-and-ops


```
三个"必须能"（缺一不上线）
  一条命令部署 / 5 分钟内回滚【仅代码】 / 出事时知道出事了
    （带 schema 变更的发布不承诺回滚，靠 expand-contract 分步发布）

流水线
  提交 → 装依赖 → 测试全绿 → 构建(带版本) → 部署 → 冒烟 → 失败自动回滚

数据库变更三步走（永远让新旧代码能同时跑）
  加新列 → 双写回填 → 切读删旧列（分三次部署）

发布策略
  先：整体替换 + 快速回滚
  最有性价比的加项：【特性开关】（部署与发布解耦，出事只关开关）

监控三级
  活着吗 / 跑得好吗 / 出事了吗 → 告警必须可执行 + 分级 + 验证过

事故响应
  先止血（回滚/关开关）→ 再找因 → 记时间线 → 复盘 → 补告警和 runbook

必须存在的 runbook
  部署 / 回滚 / 恢复备份 / 每条告警对应动作 / 密钥轮换 / 联系人
```

---

## multi-agent-squad


```
三条铁律
  1. 文件是唯一共享内存（没写进文件 = 没发生）
  2. 阶段边界换窗口（别让一个 agent 干到底）
  3. 审查者必须独立于实现者（新窗口 + 新上下文）

派活三段式
  【身份】职责边界 + 越界怎么办
  【技能】SKILL.md 绝对路径（必须显式写死）
  【输入/产出】文件路径 + 完成标准

六类消息
  HANDOFF / QUESTION / BLOCKED / REVIEW / DONE / DECISION

路线图
  0 单agent文件纪律 → 1 加Reviewer → 2 加Lead → 3 加并行 → 4 按需加角色 → 5 定期体检

换窗口决策树
  下一阶段要它作一手来源？→ 留着
  完全无关？          → 回收开新的
  换目录/换平台？      → 写文件交接
  离线+范围紧？        → 派子 agent
  其它                → 压缩后继续
```

---

## payment-and-billing


```
三条铁规
  1. 绝不自研支付逻辑（不碰卡号）
  2. Webhook 是唯一事实来源（验签 + 幂等）
  3. 一切幂等（重发是正常行为）

订阅状态机
  trialing / active / past_due / canceled / expired / paused
  必决定：扣款失败怎么办（宽限期+重试+再降级）
          取消何时生效（推荐当期结束，界面写清日期）
          升降级怎么算钱（界面必须显示金额）
  权限判定 = 查状态 ∈ 有权限集合（不许用时间推算）

Webhook 模板
  验签 → 取事件id → 幂等检查 → 【先存原始事件】→ 更新状态 → 记录 → 200
  要处理：乱序 / 重复 / 失败 / 延迟

按量计费
  计量(只增不改) → 扣减(幂等) → 阈值告警 → 硬上限(必须有) → 每日对账

上线前
  测试模式全链路 → 真实小额一笔 → 确认钱/状态/权限/收据/退款全对
```

---

## pricing-and-monetization


```
四个数
  ARPU 单价 / VC 变动成本 / GM = ARPU−VC / 回本 = CAC ÷ 月GM
  盈亏平衡客户数 = 固定成本 ÷ 单位毛利
  红线：GM < 0 → 卖越多亏越多

务实演进路径
  项目制 → 一次性 → 订阅 → 按量/额度

定价四法
  1. 从价值定价（他省了多少的 10%~30%）
  2. 三档锚定（基础=走量 / 2.5X 专业=★主力 / 5X 旗舰=锚点）
  3. 锚定在"他现在的替代方案"上
  4. 早提价、老客户保原价、单次 ≤50%

必须提前定的条款
  试用期 / 续费失败 / 退款 / 取消 / 涨价通知 / 数据导出与删除 / 发票

铁规
  agent 只给方案与算式，价格由人定
```

---

## product-validation


```
唯一的问题：有没有真实的人，有这个问题，愿意付代价？

证据分级
  L0 "挺好的"                      → 不算
  L1 描述过具体场景                 → 弱
  L2 在用土办法解决（花了时间/钱）    → 中
  L3 试过现成方案并说得出为何放弃     → 强
  L4 现在愿意付钱/预付/签意向        → 决定性

G1 通过：≥5 条 L2+（含 ≥1 条 L3/L4）+ 反证已查

必问句
  "你上一次遇到这个问题是什么时候？"
  "那次你怎么解决的？"
  "这件事多久发生一次？"

产出
  problem.md / evidence.md / counter-evidence.md / validation-report.md

结论只能三选一：继续 / 转向 / 放弃
```

---

## production-readiness


```
七节，逐项闭环，每项四字段
  现状证据 / 改动 / 验证方式 / 未解决风险
  没验证不许标 ✅

A 安全   认证 / 授权(逐端点+越权实测) / 注入 / 密钥 / 依赖
B 数据   备份真的在跑 + 【恢复演练做过】/ 回滚 / 导出
C 成本   每个花钱调用有【硬上限】/ 限流 / 支出告警 / 免费层刷不爆
D 可观测 结构化日志 / 不记敏感数据 / 健康检查 / 【告警验证过】
E 故障   全超时 / 幂等重试 / 降级显式 / 回滚演练过 / 无连接泄漏
F 合规   隐私条款一致 / 删除权 / 退款条款（人决定，agent 只列问题）
G 部署   一条命令 / 环境分离 / 证书 / runbook

状态只有三种：✅已闭环 / ⚠️部分 / ❌未做
```

---

## tech-selection


```
四个标准（按重要性）
  1. 稳定性证据（大量生产用户 / 扛过一次 breaking change / 维护者非孤身）← 创新预算 1 个
     注：年龄不是门槛（≥2 年且过了一个大版本周期即可）
  2. 一个人扛得住（托管优先、单体优先、类型安全）
  3. 招得到人 / 问得到答案 / LLM 熟
  4. 迁得走（标准协议、数据可导出）

只选三件事
  语言+框架 / 数据库（默认托管 Postgres）/ 部署平台（回滚 <5 分钟）
  其余跟着走，不讨论

必算
  月成本 + 你每月的维护时间 × 时薪
  10 倍用量敏感性（有断崖的收费模式 → 危险）

必写
  每个不可逆决策 → docs/adr/00XX-*.md（含"触发重新评估的条件"）

铁规
  按现实规模设计；扩展点留成接口，不提前实现
  认证、支付、数据库运维 —— 一律不自研
```
