---
name: frontend-delivery
description: "把前端做对：组件边界与状态归属、设计系统一致性、可访问性、四种状态（加载/空/错误/成功）、以及**怎么证明界面真的对**（Playwright 端到端 + 视觉/ARIA 快照）。专治 AI 写前端最常见的四种病：写死 mock 数据假装有后端、状态乱放导致到处同步、每页一套样式、无休止重渲染。当用户要\"写前端\"\"做界面\"\"搭页面\"\"修样式问题\"\"前端老是不对\"时使用。（Build frontends that are actually correct: component boundaries and state ownership, design-system consistency, accessibility, the four UI states, and how to PROVE the UI works.）"
---

你在写**用户直接看到**的那一层。它的特点是：**错了会立刻被看见**，而且**回归最便宜**（改一行 CSS 就可能弄坏别的页面）。

**核心认知**：前端不是"把数据画出来"。前端是**状态归属**问题——AI 写前端最常见的失败，全都是"状态放错了地方"的各种变体。

---

## 一、四种"AI 写前端"的病（先认病，再谈纪律）

| 病 | 症状 | 根因 |
|---|---|---|
| **假后端** | 组件里写死 `const data = [...]`，看起来能跑 | 没人告诉它数据从哪来；它先让页面"好看" |
| **状态乱放** | 同一个数据在两三个地方各存一份，到处 `useEffect` 同步 | 没定**谁是唯一真相** |
| **每页一套样式** | 颜色/间距/圆角各写各的，硬编码 `#3b82f6`、`padding: 13px` | 没有设计令牌，或令牌没被强制使用 |
| **无休止重渲染** | 改个输入框卡一下；`useEffect` 循环 | 依赖数组写错 / 每次渲染新建对象 |

**这四种病的共同点**：**都能被机器或人一眼查出来**——所以下面每条纪律都配了"怎么验"。

---

## 二、状态归属：**先决定"谁是唯一真相"**

**写任何组件之前，先回答三个问题**：

```
1. 这份数据从哪来？（服务端 / URL / 用户输入 / 本地派生）
2. 它是【服务端状态】还是【客户端状态】？
3. 谁会改它？只有一个地方能改吗？
```

| 类型 | 例子 | 该放哪 | 纪律 |
|---|---|---|---|
| **服务端状态** | 订单列表、用户资料 | 查询层（React Query / SWR / 框架的 loader） | **不要复制到本地 state**——那是"两处真相"的起点 |
| **URL 状态** | 当前筛选、页码、选中的 tab | **URL（query params）** | 能被分享/刷新/前进后退，**别塞进组件 state** |
| **表单状态** | 输入中的值 | 表单库或组件本地 | 只在提交时"提升" |
| **派生状态** | 过滤后的列表、总价 | **算出来，不要存** | 存了就要同步，同步就会不一致 |
| **跨组件共享的 UI 状态** | 全局提示、主题 | 一个 store（**只有一个**） | 别一个页面一个 store |

### 硬规矩

1. **派生状态不许存。** `const total = items.reduce(...)` — 不要 `useState(total)` 再 `useEffect` 去同步它。
   > 这条能消灭一半的"状态不同步" bug。
2. **服务端状态不许复制进本地状态。** 用查询层的缓存，不要在 `useEffect` 里 `setData(fetch(...))`。
3. **能被 URL 表达的状态，就放 URL。** 筛选/分页/排序/tab 都属于此类。
4. **一个数据只有一个写入点。** 如果两个地方都能改它，你迟早会遇到"改了 A 看不见 B"。

**怎么验**：
- 全局搜索 `useEffect` ——**每一个都要能说出"它在同步什么"**。说不出就是状态放错了。
- 搜索同一个字段名出现在几处 —— 超过 2 处（定义 + 使用）就要怀疑。

---

## 三、组件边界：深模块思想用在前端

沿用 `codebase-design` 的**深模块**：**小接口，大量行为藏在里面**。

| 好 | 坏 |
|---|---|
| `<OrderList status="paid" onSelect={...} />` | `<OrderList orders={orders} isLoading={l} error={e} onRetry={r} selectedId={id} onSelect={s} sortBy={sb} onSort={os} page={p} onPage={op} />` |
| 组件内部自己取数据、管加载/错误 | 把 15 个 prop 从三层之上传下来（prop 钻孔） |

**判据**：
- **props 超过 7 个** → 大概率该在组件内部自己取数据，或拆成两个组件
- **同一批 props 在多个组件间重复传递** → 那是"数据泥团"（Fowler），该变成一个上下文或查询
- **组件名说不清它是什么** → 它的职责还没想清

**拆分的信号**（出现任一条就拆）：
1. 它有**两个以上不相关的状态**（如"列表 + 弹窗"）
2. 它同时**取数据**和**做复杂布局**
3. 它的测试要 mock 超过 3 个东西

---

## 四、设计系统：**令牌化，且强制使用**

**问题**：AI 每写一个新页面就发明一套颜色和间距 → 界面"能用但不像一个产品"。

**做法**：

1. **定义令牌**（放进 CSS 变量或主题文件），命名**语义化**而不是描述外观：
   ```
   ✅ --color-danger / --space-2 / --radius-md
   ❌ --red-500 / --padding-8px     ← 描述外观，换主题就全废
   ```
2. **禁止组件里出现裸值**：不许 `#3b82f6`、不许 `padding: 13px`、不许 `border-radius: 6px`。
   > 例外：**一次性的布局微调**（如 `margin-top: 2px` 对齐）可以，但**颜色和字号绝不允许裸值**。
3. **一个组件库，不要两个**。要么全用组件库，要么全自研——混用是最常见的"看起来不统一"来源。

**怎么验（可自动化）**：
```bash
# 找裸颜色值（应只出现在令牌定义文件里）
rg -n '#[0-9a-fA-F]{3,8}\b' src --glob '!**/tokens.*' --glob '!**/theme.*'
# 找魔法间距
rg -n 'padding:\s*\d+px|margin:\s*\d+px' src
```
**允许列表之外出现裸值 = 设计系统没生效。**

> ⚠️ 上面的命令用的是 `rg`（ripgrep）—— **本机实测没装**（`rg` 会报 not recognized）。
> 没装时用 PowerShell 原生等价写法（本机实测可跑）：
> ```powershell
> Get-ChildItem src -Recurse -File -Include *.css,*.scss,*.tsx,*.ts |
>   Select-String -Pattern '#[0-9a-fA-F]{3,8}\b|padding:\s*\d+px|margin:\s*\d+px'
> ```

---

## 五、四种状态：**缺一不可**（这是最常被漏的）

任何"展示数据"的地方，必须**同时**实现：

| 状态 | 要求 | 常见漏法 |
|---|---|---|
| **加载中** | 骨架屏 / 转圈，**不白屏**；超过 ~1s 才显示（避免闪烁） | 只在本地快的时候测过 |
| **空** | 告诉用户"还没有数据" + **下一步怎么做** | 一片空白，用户以为坏了 |
| **错误** | 说清**发生了什么** + **能做什么**（重试/返回） | 静默失败或"未知错误" |
| **成功** | 正常路径 | — |

**外加两条**：
- **乐观更新要能回滚**（提交失败时界面退回去，不要停在乐观状态）
- **重复提交要防**（按钮禁用 + 服务端幂等）。**用户会双击。**

**怎么验**：每个列表/详情组件，**在测试或 Storybook 里手动触发这四种状态**（把请求 mock 成 pending / empty / reject / resolve）。
> 这一步能抓住近一半的"上线才发现"的问题。

---

## 六、可访问性：最低线（不是可选项）

不用做到完美，但这五条**必须**有——它们同时改善**所有人**的体验：

1. **语义标签**：按钮用 `<button>`，不要 `<div onClick>`（键盘和读屏都用不了）
2. **可聚焦**：所有交互元素 Tab 能到；焦点可见（别 `outline: none` 了事）
3. **表单有标签**：`<label>` 关联，或 `aria-label`；错误信息与字段关联
4. **图片有 alt**：装饰性图片用 `alt=""`
5. **颜色不是唯一信息载体**：错误不能只靠红色，要有文字/图标

**为什么它同时是"可测性"问题**：语义化做好之后，**Playwright 可以用角色+名称定位元素**（`getByRole('button', { name: '提交' })`），比 CSS 选择器稳得多——**界面重构不会弄坏测试**。

---

## 七、怎么证明界面真的对（**前端专属的验证纪律**）

前端的反馈回路比后端长得多。**"我改了代码"和"界面真的对了"之间隔着一整条链路**——所以要显式建回路。

| 层次 | 手段 | 抓什么 |
|---|---|---|
| **1. 类型/编译** | `tsc --noEmit` | 传错 prop、字段名错 |
| **2. 组件测试** | Testing Library / Vitest | 逻辑、四种状态、交互 |
| **3. 端到端** | **Playwright** | **真的能走通核心链路**（登录→主流程→付款） |
| **4. 视觉/语义快照** | Playwright 截图对比 / **ARIA 快照** | 样式意外回归；**语义结构变了** |
| **5. 人工一眼** | 截图给人看 | 只有人能判"丑不丑、像不像一个产品" |

**纪律**：

- **不许用"我看代码觉得对"当验证。** 必须至少跑到第 3 层（端到端）走一次核心链路。
- **ARIA 快照**特别适合 AI 场景：它把页面的**语义结构**固定下来，
  比像素对比稳定（不受字体渲染差异影响），又能抓住"我重构 UI 时把按钮改成了 div"这类退化。
- **视觉快照要限定区域**，别全页对比（否则内容一变就红，很快就没人看了）。
- **发布前跑一次真实浏览器**，不要只信组件测试——它们跑在 jsdom 里，没有真实布局和网络。

**终检清单**（上线前逐条过）：
```
□ tsc --noEmit 通过
□ 组件测试通过（含四种状态）
□ Playwright 跑通核心链路（真浏览器）
□ 移动端宽度下不溢出（至少 375px 看一眼）
□ 键盘能走完核心链路（Tab + Enter）
□ 控制台无错误/无警告
```

---

## 八、反模式清单

1. **组件里写死 mock 数据** → 假装有后端。**要么真调接口，要么显式标注 `// TODO: 接后端` 并让它在界面上可见**（"演示数据"标记）。
2. **`useEffect` 同步状态** → 派生状态算出来即可；服务端状态用查询层。
3. **`useEffect` 依赖数组漏项/乱写** → 无休止重渲染。**依赖写全，或用 lint 规则强制。**
4. **在渲染里新建对象/数组当 prop** → 每次渲染都变 → 子组件永远重渲染。用 `useMemo` 或提到组件外。
5. **裸颜色/魔法间距** → 设计系统失效。
6. **`<div onClick>`** → 不可键盘操作、读屏读不到。
7. **`outline: none` 去焦点框** → 键盘用户彻底迷路。要自定义就换成可见的替代样式。
8. **只做成功状态** → 加载/空/错误三态必漏。
9. **测 CSS 选择器** → 重构就红一片。用 `getByRole` / `getByLabelText`。
10. **"本地看着对"就算完成** → 真实浏览器 + 真实数据才算（对齐 `fullstack-delivery` 的"部署通过"要求）。

---

## 九、怎么跟其他技能配合

| 什么时候 | 加载什么 |
|---|---|
| 前端要接后端 | `fullstack-delivery`（契约先行：API 契约写成文件，前后端按契约并行） |
| 前端逻辑复杂（状态机、reducer） | `prototype` 做一次性原型验证状态模型，再落地 |
| 前端有难 bug / 性能回归 | `diagnosing-bugs`（**先建一个能对它变红的回路**） |
| 快上线了 | `production-readiness`（前端的检查项在 G 节之外，主要看错误上报与性能） |
| 要证明"用户真能走通付款" | **Playwright**（见 `payment-and-billing` 的上线前验证） |
| 界面要给真人看之前 | **截图给人确认**——这是唯一能判"像不像一个产品"的手段 |

---

## 十、审美与素材：**能被机器判的** + **素材由你把关**

> **来源与边界**：这一节参考了 `Leonxlnx/taste-skill`（MIT，快照 2026-08-24）。
> 那份技能是给**落地页 / 作品集 / 改版**写的，它自己声明"**不覆盖** dashboard / 数据表 / 多步产品界面"
> ⇒ 下面 **A、B 通用**，**C 只在你真做落地页或作品集时用**。
> 本库按自己的口径重写了规则（栈无关、每条配验证），**未逐字复制**；明确不采纳的三条见 D。

### A. 开工第一件事：**问用户要不要生成素材**（agent 不许自己调图像接口）

**硬规矩**（这是流程，不是审美偏好）：

1. **动笔写页面前先问一句**："这一版要图像素材吗？（主视觉 / 产品图 / 纹理背景 / 图标 / 头像）"
2. 用户说要 ⇒ **agent 不许自己去调图像生成接口**。那是**花钱的对外调用**，成本和版权都要用户当场拍板；
   改为**交出素材清单 + 每条的提示词**，素材**由用户自己生成**后放回约定路径。
3. 用户说不要 ⇒ 走**无图方案**：排版 / 留白 / 色块 / 图案撑版面。
   **拿空 `div` 假装截图，是"塑料味"最主要的来源。**

**素材清单要写成表**（每条都能被独立执行 —— 用户拿着就能去生成）：

| 列 | 写什么 |
|---|---|
| 用途 | 放哪一节、承担什么信息（"主视觉" / "网格第三格纹理" / "空状态插画"） |
| 路径与命名 | `assets/hero-<语义名>.webp` —— **语义化命名**，别叫 `img1.png` |
| 尺寸与比例 | 像素 + 比例（主视觉常用 1600×900；卡片方图；头像小方图） |
| 交付格式与上限 | webp / jpg；**单张体积上限写进清单**（别等做完才发现太大） |
| 提示词 | 用下面的模板 |
| 出处记录 | 哪个工具、哪次、什么许可 —— **版权不能靠记忆** |

**提示词模板**（用户抄这一行去改）：

```
<主体>，<构图与视角>，<材质与光线>，<色调与氛围>，<风格参照>，<画幅比例>，<排除项>
```

写法要点（每条都有理由，不是风格建议）：

- **先定"给谁看"，再定"长什么样"**：受众决定风格（B2B 的信任感 ≠ 消费品的活泼）。
- **具体名词打败形容词**："北欧厨房台面上的哑光黑铸铁锅、晨光从左后方打进来" ≫ "高级感厨房图"。
- **显式写留白**：主视觉要**给标题留位置**（如 `right third negative space`），否则图和字打架。
- **排除项必须写**：`no text, no watermark, no logos, no UI mockups`。
- **同一页的素材共享同一句"色调与光线"**：把那句复制进每条提示词，别一张一个调子。
- **一次一张、改一句再生成**：一次生成四张再挑，等于放弃可控性（花的还是你的钱）。

**素材回来之后，agent 做这些**：转 webp、`<img>` 带 `width`/`height`（防布局跳动）、
首屏之外的图 `loading="lazy"`、`alt` 按第六节；并把"谁生成的 / 什么许可"记进项目文档。

### B. "AI 塑料味"里**能被机器判**的那些（每条配验证）

| 塑料味 | 判据 | 怎么验 |
|---|---|---|
| AI 紫渐变当主视觉 | `#8b5cf6` / `violet-5xx` / `from-purple-` | 见下 |
| 假人假公司 | `Jane Doe` / `John Smith` / `Acme` / `Lorem ipsum` 出现在**要交付的文案**里 | 见下 |
| z-index 跳量级 | 出现**四位数以上**的值，且**不在项目自己的层级表里** | 见下 |
| 动效无节制 | 有动画，却查不到 `prefers-reduced-motion` | 见下 |
| 主视觉把 CTA 挤出首屏 | CTA 需要滚动才看得到 | Playwright |
| 主视觉顶部巨空 | 顶部内边距 > 6rem | Playwright |
| 桌面导航两行 | 导航换行，或高度 > 80px | Playwright |

**命令**（本机**没装 `ripgrep`**，所以用 PowerShell 原生的；装了的话把 `Select-String` 换成 `rg -n` 即可）：

```powershell
$src = 'src'   # 换成你的前端源码目录
Get-ChildItem $src -Recurse -File -Include *.css,*.scss,*.tsx,*.ts,*.jsx,*.js,*.vue,*.html |
  Select-String -Pattern '#8b5cf6|from-purple-|to-pink-|violet-5|Jane Doe|John Smith|Acme|Lorem ipsum|z-index:\s*[0-9]{4,}'
# 声明了动画却查不到这条 ⇒ 缺（无 prefers-reduced-motion 的动效对前庭敏感人群是伤害，不只是体验问题）
Get-ChildItem $src -Recurse -File -Include *.css,*.scss | Select-String -Pattern 'prefers-reduced-motion'
```

**几何类用 Playwright 断言，别靠肉眼**（接第七节第 3、4 层）：

```js
// 主视觉的 CTA 必须在首屏内；桌面导航单行且 ≤ 80px
await expect(page.getByRole('link', { name: /开始|试用|联系/ })).toBeInViewport();
const nav = await page.locator('header').boundingBox();
expect(nav.height).toBeLessThanOrEqual(80);
```

> 允许列表之外出现这些特征 = 没治干净（与第四节同一个口径）。
> 它们的价值不在"好看"，而在于**把"AI 味"从主观形容变成一次 grep 或一条断言**——
> 人只会看一次截图，机器每次都会看。

### C. 只在你真做**落地页 / 作品集**时适用

> ⚠️ 别把这组往**产品型界面**上套：后台、数据表、多步表单该守的是第二～六节。

- **主视觉是一个瞬间，不是功能清单**：文字元素最多四件（小标签 / 标题 / 副文案 / 按钮组），副文案要短。
- **"被谁使用"的 logo 墙属于主视觉下方**，不许塞进主视觉里。
- **同一布局族一页最多出现一次**（左图右文 / 三卡 / 全宽引用…），"左图右文"最多连续两节。
- **网格格子数 = 内容条数**：三条内容就三格，**不许留空格子凑版式**。
- **小标签别泛滥**：每个小节都顶一个"小号大写标签"是最典型的 AI 版式；上限约为**每三节一个**。
- **改版不是重画**：先审计（URL 结构、导航标签、表单字段名、品牌字标、法务文案都要保留），
  再决定"局部演进"还是"全量重做"。

### D. 来源、许可与**明确不采纳**的三条

- 参考：`Leonxlnx/taste-skill`（MIT，`Copyright (c) 2026 Leonxlnx`），**快照 2026-08-24**。
  ⚠️ 它标着 v2 **experimental**（接口会变）⇒ 只借鉴规则，不引用正文。
  本节文字为本库重写（**未逐字复制**，MIT 的保留声明义务不触发），来源记在
  [`THIRD-PARTY-NOTICES.md`](../../THIRD-PARTY-NOTICES.md) 第 2 节。
- **不采纳它的三条**：
  1. 它的规则用 Tailwind 类名写（`pt-24`/`text-7xl`）⇒ 本库栈无关，一律改写成物理单位或比例。
  2. 它的「**必须**用图像生成工具造素材」⇒ 本库改为上面 A 的流程：**先问用户、素材由用户出**，
     成本与版权在使用前就由用户把控（对齐工作区铁律"花钱的调用要有硬上限"）。
  3. 它的"禁破折号"⇒ 那是它针对**英文 UI 文案**的风格洁癖，与本库的中文写作无关。

---

## 附：值得参考的开源前端技能/规范（**外部资源，非本体系内容**）

> 下面这些是**别人维护的**前端 agent 规范与技能，可作为补充阅读。
> **采用前请自行核对许可与时效**（见 `开源技术引用.md` 的选型四问）。

| 资源 | 价值 |
|---|---|
| `vlkalex/ai-agent-coding-standards` | 面向 AI 的 TypeScript / React / React Native 生产级规范，可直接并入 `AGENTS.md` |
| `AIwithSakthivel/agentic-skills` | 含 `frontend-skill` 的可插拔技能定义 |
| shadcn 生态的 Agent Skills 集合 | 与"复制式组件库"配套的技能，适合 CSS/组件规范落地 |
| `playwright` 的 ARIA snapshot | 语义回归测试的事实标准（本技能第七节主推） |
| `microsoft/playwright-mcp` | 让 agent **用浏览器自己验证自己写的页面**——把"前端闭环"接上（见 `开源生态参考.md`） |
| `Leonxlnx/taste-skill`（MIT，2026-08 快照） | "反 AI 塑料味"的规则集（眉标密度、首屏与导航几何、布局族重复、暗色/减动效…）。⚠️ 它只覆盖**落地页/作品集**；本库只吸收了**能被机器判**的那几条，见第十节 B |

**取舍原则**：**抄它们的"检查规则"（可机器验证的那种），别抄它们的库清单**——
库会过时，规则不会。
