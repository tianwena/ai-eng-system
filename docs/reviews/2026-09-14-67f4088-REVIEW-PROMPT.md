# 复审任务书 · 第九轮（审 `0444ed2` + `9636c50` + `6863582` + `67f4088`，基线 `ef1e8c3`）

> **怎么用**：在**全新窗口**里执行（独立上下文 = 本库纪律"不能既改又判"）。
> 把「任务正文」整段发给新窗口的 agent；跑完把报告存为 `docs/reviews/2026-09-14-67f4088.md`。
>
> **本轮的动机**：用户实测体感"**改一个小问题都要很久**"。先量后改，共 4 个提交：
> 自检分片并行（全量 101.6s → 36.7s）、钩子加"纯注释豁免"、`--fast` 快通道写进文档、
> 以及一次"起不来子进程算没跑成"的硬化（三档退出码 0/1/3）。
> ⚠️ 其中有一件**没查明的事**：有一次提交被钩子挡下，而改动者**把自己需要的失败细节过滤掉了**，
> 事后同类运行 6 次全绿 ⇒ 根因未知。**第 1 条就是要你试着复现它。**

---

## 任务正文（整段发给新窗口）

你是**独立复审 agent**。任务：**尝试证伪**下列声明，不是确认。库根 `<库根>`。

审 4 个提交：`git log --oneline ef1e8c3..67f4088`（`git diff ef1e8c3..67f4088` 一并看）。

### 1. 并行自检：能不能复现"被挡下那一次"，以及并行是否引入不确定性

`self-test.mjs` 现在支持 `--shard k/N` 与 `--parallel [--jobs N]`：61 条用例切成 N 片、
父进程并发起子进程，**动控制台代码页的那条单独串行**。

- **复现那一次失败**：`--parallel` 连跑 **至少 8 轮**，记录每轮退出码与 `合计` 行。
  再在**负载下**跑几轮（同时开一个吃 CPU 的进程），看是否出现失败或 `没跑成`。
- **判据不能被并行改掉**：做一次**已知能红的变异**（例如把 `verify-encoding.mjs` 的
  `".bat"` 规则里 `eol: "crlf"` 改成 `"any"`），分别用**串行**与 `--parallel` 各跑一次：
  两者都必须红，且都必须是**同一条用例**红。只红在一个模式下 ⇒ 声明不成立。
- **坏输入**：`--shard 0/0`、`--shard 3/2`、`--shard abc`、`--jobs 0`、`--jobs 999`、`--jobs -1`
  各跑一次，看它是**明确报错**还是静默跑成全量/空跑（"静默跑成空转"是最坏的一种）。
- **代码页那条真的独占了吗**：把 `SERIAL_RE` 改成不可能匹配（例如 `/ZZZ/`），跑 `--parallel`，
  看是否出现中文断言随机假红 —— 这能反向证明"独占"是必要的还是多余的（两种结论都要如实写）。

### 2. 钩子的"纯注释豁免"：判据够不够严

`scripts/git-hooks/pre-commit` 新增：暂存 diff 里 `scripts/`、`tests/` 的增删行**全部落在注释/空行**时，
跳过自检与复审提醒，只补一次 `node --check`。

请**构造真实提交**（在临时克隆里做，别动本仓库）分别验证：

- **应当豁免**：只改 `//` 注释、只改块注释里的文字、只加空行 ⇒ 快（几秒内）且打印豁免说明。
- **不该豁免**：改一行代码；改一行**字符串字面量**；改一行 `import`。
- **最关键的边界**：注释**看起来像注释、删了却会破坏语法**的情形 —— 例如删掉一行 `*/`、
  或把 `//` 换成 `/*`（后面的代码被吞掉）。这条路径只靠 `node --check` 兜底，
  请**实测**它是否真的被拦住（`.ps1` 与 `.mjs` 各试一次；注意 `.ps1` 走的是 `verify-ps1.mjs`，
  它在**豁免之前**就已经跑过了 —— 请确认这是真的，而不是"我以为它跑过了"）。
- **误判**：`grep -vE '^[+-][[:space:]]*(//|/\*|\*|#|<!--)'` 这种判据会把哪些**真代码**误当注释？
  造 3 个例子（提示方向：以 `*` 开头的表达式续行、以 `#` 开头的 shell/PS 代码、以 `<!--` 开头的字符串）。
  误判的后果是"该跑自检的没跑"——比多等 30 秒严重，请给出具体反例或明确说"没找到"。

### 3. "起不来子进程算没跑成"（三档退出码）是否真的成立

改动者称：`out.r.error` 存在 ⇒ 记 `notRun`、退出码 3；`verify-all` 会把 3 正确地上报成"没跑成"。

- 请**人为制造**一次 spawn 失败（例如临时把某个用例里的 `powershell.exe` 路径改成不存在的文件），
  确认：① 打印的是 `[没跑成]` 而不是 `[FAIL]`；② 整轮退出码是 **3**；③ 在 `--parallel` 下，
  父进程也汇总成 3；④ `verify-all` 把这一步报成"没跑成"而不是"失败"或"通过"。
- 反向检查：**真失败**（断言不成立）时退出码必须是 1、且**不许**被算成 3（把两者混起来比误报更糟）。

### 4. 真值推导改动（`check-consistency.mjs`）

改后它同时接受 `for (const x of CASES)` 与 `runCases(CASES)` 两种"被迭代"的写法。

- 变异验证：① 把用例数组改名（`CASES` → `X`）⇒ 必须自曝"没人迭代"；
  ② 把 `runCases(CASES)` 那行删掉 ⇒ 必须自曝；
  ③ 只在**注释里**写一句 `runCases(CASES)`（代码里没有）⇒ 会不会被误判成"已迭代"？
  （这是本改动引入的新风险，请**实测**并给结论。）
- 顺便核对：`从这里开始.md` 与脚本头部新写的实测数字（**36.7 / 32.6 / 4.3 秒**）在**你的机器上**
  是否也成立（口径：`Measure-Command` 或 `time`）。数字对不上就报出来 —— 库里的数字是要能核的。

### 5. 别做

- 除报告本身**不许改仓库**；发现问题写进报告，由改动者修。
- 没条件测的写"没测"，**不要**用推理代替实测。
