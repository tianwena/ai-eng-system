# 复审任务书 · 第七轮（审 `536995c` + `7f66382`，基线 `39ee6e9`）

> **怎么用**：在一个**全新窗口**里执行本任务（独立上下文 = 本库纪律要求的"不能既改又判"）。
> 把下面「任务正文」整段发给新窗口的 agent；它看得到仓库，但**不应该**看改动者的推理过程。
> 跑完把报告保存为 `docs/reviews/2026-09-13-536995c.md`（命名沿用 `<日期>-<基点sha>`）。
>
> **这轮为什么特殊**：它是**CI（GitHub Actions, windows-latest）第一次真跑**照出来的。
> 公开库 `tianwena/ai-eng-system` 首跑红三条，而**本机 `verify-all` 一直是 6/6 全绿** ——
> 也就是说：这轮修的正是"**所有本地判据集体失明**"的那一类问题。
> 复审时请特别留意：**还有没有别的"只在本机成立"的假设**（这类问题不会在本机自曝）。
>
> **本轮两个提交**：`536995c`（CI 三条红灯）+ `7f66382`（A1/A2 漏掉中文名文件）。
> 第二个是调查第一个时**顺手量出来**的同类漏网，判据与证据都在下面第 8 条。

---

## 任务正文（下面整段发给新窗口）

你是**独立复审 agent**。任务：**尝试证伪**下列声明，不是确认。库根 `<库根>`。

审**两个提交**：`536995c` 与 `7f66382`（`git log --oneline 39ee6e9..7f66382`、`git diff 39ee6e9..536995c`、
`git diff 536995c..7f66382`）。基线 `39ee6e9`。第 1–7 条针对前者，第 8 条针对后者。

背景：CI 首跑红三条（`速查卡.md 不存在`；自检 57 条里红 2 条）。改动者的结论是**两个根因**：
① `extract-cheatsheets.mjs` 写死了本机绝对路径；② `redline-check.ps1` 用 `Resolve-Path` 取根路径，
而 CI runner 的 `TEMP` 形如 `C:\Users\RUNNER~1\…`（8.3 短名）⇒ 短名/长名对不上。
你要做的是**证明这两条结论站不站得住、修法有没有引入新问题**。

### 要逐条证伪的声明

1. **根因②的机制是真的，而且修法覆盖了它**
   - 声明：`Resolve-Path -LiteralPath <8.3 短名>` **保留短名**，而 `Get-Item` / `Get-ChildItem`
     给**长名**；旧代码用 `Join-Path $root …` 拼出 `$trackedFull`、拿它和磁盘枚举的 `.FullName` 比，
     于是同一个文件被判红一次 + 又被当"未跟踪"扫一遍。
   - 请**自己构造**（别只看注释）：在 `%TEMP%`（C: 卷，默认开 8.3）下建长名目录，用 `%~sI` 取短名，
     直接量四个值：`(Resolve-Path <短>).Path`、`(Get-Item <短>).FullName`、
     `(Get-ChildItem <短> -Recurse -File)[0].FullName`、以及 `Join-Path` 拼出来的那个。
   - **新用例是不是空转**：`self-test.mjs` 里那条「路径写成 8.3 短名时也不许自相矛盾」——
     它在**没有 8.3 短名的卷**上会走哪条分支？跳过分支会不会把真失败伪装成通过？
     请把用例的跳过条件改成"永远跳过"再跑一遍，确认它**不再**能发现旧 bug（证明判据非空转）。
   - **别的路径形态**：短名只是这一类的一种。请继续找：NTFS 目录联接 / 符号链接（`mklink /J`）、
     `subst` 虚拟盘、UNC 路径（`\\localhost\C$\…`）、盘符大小写、路径里带 `..`、
     目录名带尾随点/空格。**任一种**能让"已跟踪"与"未跟踪"两轮对不上 ⇒ 修法不完整，请报出来。
   - 声明还说"两处修复互为冗余、只撤一处用例仍绿"。请**实测**：分别只撤 `$root` 那处、
     只撤"登记规范路径"那处，各跑一次自检，记录是不是 59/59。

2. **根因①的修法是"读自己所在的仓库"，而不是换个写死的地方**
   - 声明：`REF` 由 `import.meta.url` 推出；`--ref` 可覆盖；技能根由 `--installed` / `DSH_HOME` 决定。
   - 请构造：**把 scripts 目录做成符号链接**（或从别处 `node <库根>\scripts\extract-cheatsheets.mjs`），
     确认它仍然读**库根**而不是 cwd、也不是链接所在位置——说明哪种行为是"对的"以及为什么。
   - `--write` 路径这次被改了（同步结果分三档报出，未设 `DSH_HOME` 时明说跳过）。
     **不要真跑 `--write`**（它会改技能正文）；改用 `--ref` 指向一个**临时副本**跑，
     确认它只动副本，且"跳过/失败"那两档措辞与事实相符。
   - 新增用例「速查卡漂移检查读的是它自己所在的仓库」：把 `REF` 那行改回写死的**本机**路径
     （`D:\<你的库根>`），这条用例**必须红**。如果它仍然是绿的 ⇒ 判据无效。

3. **`\.git\` 显式过滤没有改变任何对外声明**
   - 声明：把 `\.git\` 从工作区枚举里显式排掉只是"把实现细节变成判据"，行为不变。
   - 请验证两件事：① 旧代码真的不会枚举 `.git`（用 `Get-ChildItem <库根> -Recurse -File` 数一下，
     对比 `-Force`）；② 新过滤**不会**改变输出里任何一句数字/结论（`扫描 N 个被跟踪文件`、
     `另外扫了 N 个未跟踪`、范围注里的各类跳过计数）。**若某个数字变了 ⇒ 说明旧注释在说谎**，请报出来。

4. **可诊断性改动不会制造新风险**
   - 声明：用例失败时总是打印子进程原始输出（`raw`，尾部 20 行）。
   - 请检查：它会不会把**真实密钥**打进日志（用它自己的夹具验证：故意让一个用例读一份含
     形似真密钥的输入，看打印出来的内容）；CI 日志体积会不会爆（估算最坏情况）；
     `-v` 的原有契约有没有被破坏。

5. **行为变更：`-Project` 指向不存在的目录**
   - 改动前 `Resolve-Path` 失败是**非终止错误**（脚本继续跑，`$root` 为空）；
     改动后直接 `exit 1` 并打一行中文。请检查：有没有**任何**用例/文档/下游脚本依赖旧行为
     （尤其 `verify-gate-integrity.mjs`、`self-test.mjs`、`从这里开始.md` 里的示例）。
     也请判断新行为是否正确（`exit 1` 而不是别的码、措辞是否与"没跑成"的语义冲突）。

6. **真值同步没有漏项**
   - `个用例` 57 → 59，改了 `facts.json` 与两份文档。请**自己数一遍** `CASES` 的条数，
     并检查还有没有别的文件在暗示用例条数（历史留档里的"那一轮是 53 条"这类**不算**，要区分开）。
   - 请复跑钩子（`git commit --amend` 之类**不要**做；直接跑 `scripts/verify-ps1.mjs`、
     `scripts/check-consistency.mjs`、`node scripts/self-test.mjs`）确认全绿。

7. **最值钱的一问：还有没有别的"只在本机成立"的假设**
   - 库里有专门一项 `no-hardcoded-paths` 一致性检查，**它没拦下这次闯祸的硬编码路径**。
     请查清它的**扫描范围**（哪些文件、什么正则、豁免了谁），并列出：
     ① 它按设计**不该**管的范围（写清理由）；② 它**该管却没管**的（= 缺口，请报出来）。
   - 这条最贵的教训是：`verify-clean-clone.mjs`（在干净克隆里跑全套）**本该**抓住根因①，
     却因为"脚本自己偷偷读了本机路径"而假绿。请评估：**还有哪些"干净环境"验证是被同一个
     花招骗过去的**（例如它有没有把 `%TEMP%`、`HOME`、短名、代码页、git 配置一起变干净）。

8. **第二个提交 `7f66382`：A1/A2 漏掉中文名文件（`git diff 536995c..7f66382`）**
   - 声明：`quality-gate.ps1` 的 A1/A2 原先用**裸 `git ls-files`**，而它默认 `core.quotePath=true`
     ⇒ 非 ASCII 路径变成带引号的八进制转义 ⇒ A1 的文件名正则、A2 的扩展名过滤双双落空。
     实测口径（**请自己构造，别信注释**）：建一个 git 仓库，放
     内容**逐字节相同**的四份文件：`asciidir/.env` 与 `中文目录/.env`、`ascii_cfg.py` 与 `中文配置.py`
     （后两份各含一行形似密钥的 `api_key = "…"`），全部 `git add`。
     期望修复态：`A1 = FAIL | 2 file(s)`、`A2 = FAIL | 2 hit(s)`。
     请**同时**在老代码上（`git stash` 或 `git show 536995c:scripts/quality-gate.ps1` 覆盖到临时副本）
     复现旧口径，确认它真的只报 1 个 —— 否则"这是个缺陷"这句话就没根据。
   - **变异**：把 `Get-GitTrackedFiles` 里的 `-c core.quotePath=false` 去掉 ⇒ 自检应当
     **恰好红 1 条**（60 → 59/60）。请实测数字，别照抄这句话。
   - **覆盖面**：作者声称"本脚本只此一份"，且 `redline-check.ps1` 里另有一份。请用 `git grep` 之类
     自己数一遍**全库**还有没有第三处裸 `git ls-files`（或等价的"读 git 输出当路径用"的地方）——
     例如 `verify-all.mjs`、`check-consistency.mjs`、`verify-clean-clone.mjs`、git 钩子里。
     有的话：它会不会也漏中文名路径？请给出最小复现或明确说"测了，不会"。
   - **诚实性**：A2 现在把"定位不到因而没扫"的文件数出来写进明细（`$missing`）。
     请构造一个**被跟踪但工作区已删**的文件，确认这句话出现在明细里、且不会被读成"清单完整"。
     也请判断：这种情形该不该是 `PASS`（作者选的是 PASS + 明说没扫，理由是不想把
     "暂存了删除"这种常态判成"没结论"）—— 你若认为该是 `NOTRUN`，请说明代价。

### 交付

- 按 `docs/reviews/README.md` 的格式写报告：每条声明 → **证伪结果**（成立/不成立/部分成立）
  → **你实际跑的命令与原始输出**（不要只写结论）→ 你**新发现**的问题（含最小复现）。
- **不许**改仓库里的任何文件（除报告本身）；发现问题就写进报告，由改动者去修。
- 若某条你**没条件测**（缺工具等），明说"没测"，**不要**用推理代替实测。
