# 复审任务书 · 第八轮（审 `ef1e8c3`，基线 `e19350f`）

> **怎么用**：在一个**全新窗口**里执行本任务（独立上下文 = 本库纪律要求的"不能既改又判"）。
> 把下面「任务正文」整段发给新窗口的 agent；它看得到仓库，但**不应该**看改动者的推理过程。
> 跑完把报告保存为 `docs/reviews/2026-09-14-ef1e8c3.md`（命名沿用 `<日期>-<基点sha>`）。
>
> **这轮的特殊之处**：本轮**大部分代码不是改动者写的** —— `scripts/verify-encoding.mjs` 是
> 另一个窗口新增的检查器，本轮只做"登记 + 接线 + 补一条负向测试 + 修它的一行死代码"。
> 所以复审要**分开看两件事**：① 那个新检查器自己的判据对不对（本轮没审过）；② 改动者这轮的收口动作对不对。
> 另外：改动者这轮**自己抓到并修掉了自己写的一条假护栏**（详见第 4 条），请独立复现那个变异。

---

## 任务正文（下面整段发给新窗口）

你是**独立复审 agent**。任务：**尝试证伪**下列声明，不是确认。库根 `<库根>`。

审**一个提交**：`ef1e8c3`（`git show ef1e8c3 --stat`、`git diff e19350f..ef1e8c3`）。基线 `e19350f`。

### 1. 先审**那个新检查器本身**（不是本轮的改动，但本轮把它接进了总闸）

`scripts/verify-encoding.mjs`（约 8.6KB，2026-09-14 由另一个窗口新增）声称把工作区
`AGENTS.md` 的「🔴 编码铁律」表变成可执行检查。请**逐条对照那张表与它的 `RULES`**，并实测：

- **规则是否与铁律表一致**：`.ps1` 要 BOM、`.bat/.cmd` 要 GBK+CRLF 且**不能有 BOM**、
  `.py/.md/.json/.txt/.yml/.yaml/.mjs/.js/.css/.html` 要 UTF-8 无 BOM。
  构造**每一种**情形的正例与反例（至少：`.ps1` 有/无 BOM、`.bat` 用 UTF-8、`.bat` 用 LF、
  `.md` 带 BOM、`.json` 带 BOM），确认**报与不报都符合表**。
- **误报**：一个**纯 ASCII** 的 `.bat` 会不会被误判？（实测：不会，但请自己跑。）
  一个**含合法 GBK 中文**的 `.bat` 会不会被误判为"无法按 GBK 严格解码"？
  一个**故意讨论 U+FFFD 这个字符**的 `.md`（正文里写真正的一字节 U+FFFD）会不会被当成"文件已写坏"？
  ——这一条尤其要判：**"损坏"与"内容里本来就有这个字符"在判据上分得开吗**？
- **它自己有没有"没跑成 ≠ 通过"的降级**：把 `-Repo` 指向一个不存在的目录、一个空目录、
  一个非 git 目录，各跑一次；期望是**明确的结论**（通过/不通过/没跑成）而不是抛异常栈。
  ⚠️ 已知一处：非 git 目录会往 **stderr** 打一行 `fatal: not a git repository…`（噪音，不影响判定）。
  请判断这是不是该修、以及**有没有更严重的问题被这行噪音盖住**。
- **它扫哪些文件**：`git ls-files` + 未跟踪；`SKIP_DIRS` 里的目录名清单是否漏了什么
  （例如 `.scratch/` 只跳了 `.scratch/tmp`；本仓库现在没有 `.scratch/`，但**别的项目会**）。

### 2. 审本轮的**接线与登记**（六个检查器 → 七个）

改动者声称：`verify-all.mjs` 加了第 4 步、`EXPECTED_STEPS` 6→7、`PINNED_VERIFY_ALL_STEPS`
同步加一行、`从这里开始.md` 标题与清单与「七个检查器」都改了、`facts.json` 的三个 registeredNumbers
（`个脚本` 19 / `个检查器` 7 / `个用例` 61）与 `verify-all-count` 的行锚点 witness 都改了。

- 请**自己数**：`git ls-files` 下的脚本数是不是 19（口径见 `check-consistency.mjs`）、
  `verify-all.mjs` 的 `steps` 是不是 7、`CASES` 是不是 61。
- **漏网的旧数**：跑 `git grep -n "18 个脚本\|6 个检查器\|57 例\|60 个用例"` 全库找一遍
  （登记机制只认**登记过的名词**，`…例` 这类写法它看不见 —— 改动者已经手工找到一处
  「自检 57 例」并改了，请确认**还有没有别的**，包括 `docs/reviews/` 里那些**历史留档**
  该不该改（那些是当时的记录，改了就是篡改 —— 请给出判断，别一律改）。
- **顺序**：`PINNED_VERIFY_ALL_STEPS` 的顺序必须与 `verify-all.mjs` 的步骤顺序一致吗？
  实测：把两处顺序调换（内容不变）会不会红？如果不会红，说明这份"钉住"漏了一个判据（请报出来）。

### 3. 审两处 U+FFFD 的处置

`scripts/self-test.mjs:603` 与 `tests/fixtures/BEHAVIOR-VERIFICATION.md:139` 各有一行引用
"当时控制台乱码样本"的文本，含 U+FFFD。改动者查历史后判定**不可恢复**，换成了明确占位。

- 请独立复核"不可恢复"这个结论：`git log -S` / `git show` 翻一遍那两个文件的历史，
  以及**同一段诊断是否在别处还留着可读版本**（例如 `docs/reviews/` 的某份报告）。
  如果其实能找回原文 ⇒ 改动者的处置是错的（丢掉了可恢复的证据）。
- 判断占位文案是否**诚实且够用**：它保留了"结构看得到、中文已丢"这个信息吗？
  有没有更该保留的东西（例如换成一句"见 commit `b3514fc`"的指路）？

### 4. 独立复现那条负向测试与它的变异（**最重要的一条**）

改动者声称：新用例「编码/行尾体检：BOM 与行尾装反了必须被抓到」通过；
把 `verify-encoding.mjs` 里 `.bat` 的 `eol: "crlf"` 改成 `"any"` ⇒ **61 条里恰好红这一条**（60/61）。

- 请**原样复现**这个变异，记录真实数字，别照抄上面这句。
- 再试**两种反向变异**，确认这条用例不是"因为错误的原因而绿"：
  ① 删掉 `.md` 的 `bom: "forbidden"`；② 把 `.bat` 整条规则从 `RULES` 里删掉。各自跑一次。
- ⚠️ 改动者第一版夹具用 `"ascii"` 编码写中文，中文被截断成非法字节 ⇒ 触发**另一条**规则
  （"无法按 GBK 严格解码"）⇒ **删掉 EOL 规则用例照样绿**（假护栏）。请验证修好的版本
  是否真的只对"裸 LF"敏感：**把夹具里的 `.bat` 改成 CRLF（内容不变）⇒ 用例应当变红**
  （因为它断言"必须有 `[ERROR] lf.bat`"）。如果它照样绿 ⇒ 还是假护栏。
- 另外：如果 `scripts/verify-encoding.mjs` 被整个删掉，这条用例会不会**假绿**？
  （`spawnSync` 的 `status` 是 null，而用例写的是 `r.status ?? 1` —— 请实测。）

### 5. 别做

- **不许改仓库里的任何文件**（除报告本身）；发现问题写进报告，由改动者去修。
- 某条没条件测就写"没测"，**不要**用推理代替实测。
