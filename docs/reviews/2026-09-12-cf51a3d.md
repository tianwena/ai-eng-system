# 复审记录：`cf51a3d`（把"改体系必须过独立审查"落成纪律）

> **这是本目录的第一份记录**，也是"复审留痕"这条**推荐**做法的示范。
> **只记事实**：审了什么、发现什么、怎么处置（含**拒绝**的建议和理由）。
> **不评判**"这次改动值不值得做"——那是人的决定。

| 项 | 内容 |
|---|---|
| **基点** | `fade514`（自检套件入库）→ 被审改动 `cf51a3d`（纪律 6 落地） |
| **复审者** | 独立上下文的 subagent，**未参与改动**，**未修改任何文件** |
| **给它的材料** | `git diff fade514..cf51a3d`、相关文件、两个跨仓库 `AGENTS.md` |
| **给它的说法** | 只给"声称解决的问题"一句话，**不给推理过程** |
| **它自己做的验证** | 在临时仓库里实测钩子正则的触发条件；核对了全部 15 项一致性与 11 个自检用例 |

## 它发现的（以及我怎么处置）

| # | 发现 | 处置 |
|---|---|---|
| 1 | **改名/移动检查器 → 钩子不触发**，自检与复审提醒**双双静默失效** | 我在此之前已自己跑到并修了（`88c634f`）；复审进一步指出**根因是"触发集是手写名单"** → **采纳**，改为目录前缀 `scripts/` + `tests/`，整类漂移消失 |
| 2 | 改名后报错信息说成"内容一致性未过"，**方向错误** | 已修（`88c634f` 前置检查）。复审的独立复述确认了这个判断 |
| 3 | **文档承诺的范围 > 钩子视野**：文档说"检查器/闸门/技能正文"，钩子只管 4 个 `.mjs` | **采纳"收窄文档"而不是"扩宽钩子"**：定成两档（重装 = `scripts/`+`tests/`；轻量 = 技能正文/文档）。理由：给每条技能措辞改动都要求独立复审，只会让这条规矩被忽略——和 G1–G4 太重的教训同源 |
| 4 | **提醒排在自检之后** → 自检没过就永远不提醒；改完再用别的文件提交则彻底没有提醒 | **采纳**：提醒移到自检**之前**，并在失败分支里写明"复审义务依然成立，且不会再提醒" |
| 5 | `从这里开始.md` 里"下面这四条/上面四条"与钩子实际的 5 个步骤**对不上** | **采纳**：改成非数字措辞（本库自己的规矩：**没有机器来源的数字不要写**） |
| 6 | **我自己的实证被夸大了**：新文档写"三次 bug 全靠独立 agent 发现"，而 `self-test.mjs` 头注释写的是"三次都是人肉发现" | **采纳，且这是本次最有价值的一条**。核实后：**两次**（加粗错误行、铺垫当端点）确实由独立 agent 在真实项目上跑出来；**一次**（统计正则只匹配到 2/10 行）是我自己写负向测试发现的。三处文字已按事实改写，纪律 6 的理由改成"需要另一双眼睛**或**一个会失败的测试" |
| 7 | **钩子本身零机器覆盖**：收窄正则或删掉提醒，所有闸门仍全绿 | **采纳**：`self-test.mjs` 新增 3 条钩子用例（直接**从钩子里抽出真正则**来跑，不另抄一份），并**对它们做了负向测试**——故意弄坏不变量，确认每条用例都会红（否则它们就是"永远为真"的假检查） |
| 8 | 触发集与 `script-inventory`（13 个脚本）**不同源**，新增检查器时必然静默分叉 | **采纳**（由 #1 的目录前缀方案一并解决） |
| 9 | 两个 `AGENTS.md` 与库内纪律 6 **无同步检查** | **暂不采纳**：跨仓库文件，做检查要把本机绝对路径写进库里；价值低于成本。**记录在案**，若再次发生漂移再处理 |
| 10 | `git commit --no-verify` 绕过时**没有留痕** | **部分采纳**：在入口文档写明"绕过 = 同时绕过复审提醒"；不做事后留痕（会变成形式主义） |
| 11 | "复审是否真的发生过"**无凭据**，最坏情况是同一上下文"顺手当一次 reviewer" | **部分采纳**：定 `docs/reviews/<日期>-<基点sha>.md` 为**推荐**（不强制）——强制只会制造"看起来审过"的空文件，比不审更糟。本文件即示范 |

## 它明确说"无法验证"的（我没有替它下结论）

- 那三次检查器 bug 的**历史发现方式**（仓库里当时只有两种互相冲突的措辞，无第三方记录）。
  → 我按事实来源核实后按 #6 改写；**但这条核实依赖我自己的会话记忆，仍不是可追溯证据**。
- "自检全绿 ≠ 判据选对了"这一断言**本身**在库内没有证明（是推论，合理但未证）。
- reviewer 提示词模板**在实战中的有效性**：本目录此前**没有一份用它产出的记录**（本文件是第一次使用）。

## 这次复审本身证明了什么

**它抓到了我抓不到的东西**：第 3、4、6 条我都没想到（尤其第 6 条——我在无意中给一条纪律编了
比事实更强的理由）。这不是"再看看就能发现"的疏漏：**写的时候我就认为那是真的**。

> **结论**：`self-test.mjs` 证明"判据仍然成立"；**独立复审才能质疑"判据选得对不对"**。
> 两者不是替代关系，缺一个都会留下自认看不见的盲区。
