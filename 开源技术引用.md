# 开源技术引用

本体系**不是从零发明的**。它站在一批开源项目、方法论与公开标准之上。
本文记录**用了什么、用在哪、各自解决什么问题**，以及**引入第三方依赖时的纪律**。

> 许可全文与逐项归属见 [`THIRD-PARTY-NOTICES.md`](./THIRD-PARTY-NOTICES.md)。

---

## 一、技能与指令格式

| 来源 | 用在本体系的哪里 | 解决什么问题 |
|---|---|---|
| **MCP 之外的 "Skills" 格式**（`SKILL.md` = YAML frontmatter + 指令正文） | 全部 36 个技能的文件形态 | 让"给 agent 的行为规范"可版本化、可按需加载、可跨 harness 迁移 |
| **Skill 调用策略**（user-invoked / model-invoked 二分） | `README.md` 机制 1、所有技能的 frontmatter | 区分"编排入口"（只由人触发）与"可复用纪律"（模型可自动够到） |
| **Agent 指令文件约定**（`AGENTS.md` / `CLAUDE.md` 分层加载） | `DSH-INTEGRATION.md`、工作区与全局 `AGENTS.md` | 让 agent 在会话启动时获得"按目录逐层具体化"的项目纪律 |

---

## 二、方法论来源

| 来源 | 用在本体系的哪里 | 解决什么问题 |
|---|---|---|
| **领域驱动设计（Eric Evans）** — 统一语言 / 术语表 | `domain-modeling`、`CONTEXT.md` 机制、`术语对照表.md` | agent 被扔进项目里自己猜术语 → 用 20 个词说 1 个词的事 |
| **《程序员修炼之道》** — 曳光弹 / 小而审慎的步骤 / 反馈速率 | `to-tickets`（垂直切片 + 阻塞边）、`tdd` | 大块工作无法验证；缺少反馈回路 = 盲飞 |
| **《重构》（Martin Fowler）** — 代码坏味道清单 | `code-review` 的**标准轴**（12 条坏味道基线） | 给"这段代码不对劲"一个可共享的、有名字的词汇表 |
| **《软件设计哲学》（John Ousterhout）** — 深模块 | `codebase-design`、`improve-codebase-architecture` | 用简单接口访问大量功能；避免浅模块与泥球 |
| **扩张-收缩（expand–contract）迁移模式** | `to-tickets` 的"大范围重构"例外、`launch-and-ops` 的数据库变更三步走 | 一次性机械改动炸穿全库时，没有任何垂直切片能绿着落地 |
| **红-绿-重构（TDD）** | `tdd`、`fullstack-delivery` 的交付单元定义 | 先写会失败的测试，再写实现 |
| **刺猬式证据分级**（证据强度分档） | `product-validation` 的 L0–L4、`commercial-squad` 的 G1 判据 | 把"朋友说不错"和"已经付了钱"区分开 |
| **AARRR / 队列留存 / time-to-value** | `growth-and-analytics` | 从"总量好看"转向"留存与到达价值的时间" |
| **单位经济模型**（ARPU / VC / 毛利 / CAC / 回本） | `pricing-and-monetization`、`tech-selection` 成本模型 | 卖得越多亏得越多；渠道要按"留存后成本"评估 |

> 本体系对上述方法论做了**面向"单人 + agent 团队"的重写**：加了可执行判据、期限、反模式与 DSH 机制映射。

---

## 三、运行时与工具技术

| 来源 | 用在本体系的哪里 | 解决什么问题 |
|---|---|---|
| **DeepSeek Harness（DSH）** 的子 agent / 工作流 / 后台作业 / 中断机制 | `multi-agent-squad` 的"harness 机制映射"表 | 把六类消息协议落到**真实可用**的机制上，而不是纸面协议 |
| **Chokidar**（文件监视） | 技能目录的热更新与失效（DSH 内部使用） | 技能改动后被重新发现 |
| **YAML**（`yaml` 包） | frontmatter 解析；体检器**复用 DSH 同一个解析器** | 保证"体检结论"与"DSH 实际行为"一致 |
| **PowerShell 5.1 / 7+** | `wizard` 的 `template.ps1` | 在 Windows 原生可用，不依赖 bash/WSL |

---

## 四、外部工具链（**建议接入，非内置**）

这些是 [`production-readiness`](./engineering/production-readiness/SKILL.md) 与 [`launch-and-ops`](./engineering/launch-and-ops/SKILL.md) 里
"把手工清单变成可执行闸门"所指向的工具。**本体系不内置它们，只规定该在哪些环节用。**

| 环节 | 工具 | 作用 |
|---|---|---|
| 依赖漏洞 | `pip-audit` / `npm audit` / **Trivy** | 依赖与镜像漏洞扫描 |
| 静态分析（SAST） | **Semgrep** | 注入、鉴权漏判等模式化缺陷 |
| 密钥泄露 | **gitleaks** / trufflehog | 扫 git 历史与提交时 |
| 端到端验证 | **Playwright** | 唯一能证明"用户真能走通付款流程"的手段 |
| 负载 | k6 | 上线前知道并发下会不会挂 |
| 错误追踪 | **Sentry**（可自托管） | 上线后必须知道客户那边报什么错 |
| 大模型成本闸门 | **LiteLLM** 网关 | 硬预算、限流、每用户 token 成本 |
| 前端/后端脚手架 | 自选（如 `open-saas`、`fastapi/full-stack-fastapi-template`） | 鉴权/支付/邮件不自研 |

> **选型背景与"星高但已死"的坑**见 [`开源生态参考.md`](./开源生态参考.md)。

---

## 五、引入第三方依赖的纪律

**看中任何开源项目之前先过这四问**（也写进了工作区 `AGENTS.md`）：

```
1. 它建在【标准协议】之上吗？（Postgres / S3 / SMTP / OIDC / MCP / Stripe 兼容 API）
   → 是 → 锁定风险低，随时能换；否 → 被绑架，换的成本可能等于重写

2. 最近 6 个月有没有提交和发版？
   → 打开仓库看 Commits / Releases 的时间戳，不要只看 star

3. 它解决的是【当前瓶颈】吗？
   → 卡在"没人要"（G1）时引入任何工具都是浪费

4. 它是【方法论型】还是【运行时型】？
   → 方法论型：随便借鉴，零运维成本，可弃
   → 运行时型：当结婚对象考虑——每引入一个，多一份长期负债
```

**硬约束**：

- **引入运行时型项目前必须问用户**（对齐铁律"改动必须经过用户确认"）。
- **商业产品必须自己确认许可证**——免费模板常有附加条款；`n8n` 一类是 Sustainable Use License，转售需谨慎。
- **别信 star 数**：已有多个"星很高但已死"的例子（`gpt-engineer` 已归档、`MetaGPT` 已停维护、`openai/swarm` 只是教学框架）。
