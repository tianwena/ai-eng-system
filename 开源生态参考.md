# 开源生态参考（截至 2026-09-12）

> **目标读者**：**一个人 + AI 编码代理（DeepSeek Harness / Claude Code 类）**，要把产品真正做出来并上线卖钱。
> 调研日期：**2026-09-12**

---

## 0. 先读这一节：可信度声明 + 选型纪律

### 0.1 我怎么取的星标数（不确定性必须诚实标注）

调研环境的 shell **出网被阻断**（`curl` 对 `example.com` 报 SSL exit 35，对 `google.com` 超时，GitHub API 不可达），因此**无法用 GitHub API 核实**。所有星标数来自搜索命中的第三方星标追踪站的**页面标题快照**（star-history.com 及其镜像、whatstrending.ai、sourcepulse.org、git-stars.org、githublb.vercel.app 等）。

**因此**：

1. **星标数是快照，不是实时值**，且不同镜像之间会不一致（曾见过 `litellm` 同一天出现 49.9k 与 57.7k 两个值）。**量级可信，精确值请自己打开 GitHub 页面。**
2. **"活跃维护"是间接推断**（2026 年仍有新版本/新讨论），**没能逐个仓库核实最后 commit 时间**。凡存疑处都标注了。
3. 标 **"未核实"** 的星标 = 没拿到可靠快照，**请自行确认**。
4. **许可证未逐仓核实。** 商业产品**必须自己确认**——重点看 `open-saas`（免费模板常有附加条款）、`n8n`（Sustainable Use License，**转售需谨慎**）、`Kodus`、`OpenClaw`（生态大，许可可能分层）、`CrewAI`/`Agno`（开源版与企业版功能切分）。

### 0.2 选型纪律：四个问题（比清单更重要）

看中任何一个项目，先问这四句。**过不了就别用。**

```
1. 它建在【标准协议】之上吗？（Postgres / S3 / SMTP / OIDC / MCP / Stripe 兼容 API）
   → 是 → 锁定风险低，随时能换
   → 否 → 你被它绑架，换的成本可能等于重写

2. 最近 6 个月有没有提交和发版？
   → 打开仓库看 Commits 与 Releases 的时间戳（别只看 star）

3. 它解决的是【你的瓶颈】吗？
   → 你现在卡在 G1（没人要）还是 G3（做不出来）？
     卡在 G1 时引入任何工具都是浪费——先去找人聊。

4. 它是【方法论/技能】还是【要长期运行的软件】？
   → 方法论：随时可借鉴，零运维成本，可弃
   → 软件：一旦进生产，你就得跟着它升级、踩它的坑、付它的账单
```

**核心判断**：
> **"方法论型"项目可以随便抄，"运行时型"项目要当成结婚对象。**
> 一个人维护项目，第 4 类要**非常克制**——每引入一个运行时，你都多了一份长期负债。

---

## 1. 最重要的发现：2026 年霸榜的不是编排框架，是"技能层"

**两条主线**：

1. **微软把 AutoGen 和 Semantic Kernel 合并成了 `microsoft/agent-framework`**，旧的 AutoGen 路线进入维护/迁移状态。把两者当两条并行路线选型**已经过时**（[InfoWorld](https://www.infoworld.com/article/4199575/the-microsoft-agent-framework-wars-are-over-the-real-architecture-decision-starts-now.html)）。
2. **真正霸榜的是"技能（Skills）/代理行为规范"层**——`obra/superpowers`（278.6k）、`anthropics/skills`（174.4k）都在全球前列。这说明社区已经发现：**代理不可靠的瓶颈不在编排拓扑，而在"代理被允许怎么做"的约束层。**

> **这对你是极重要的信号**：**不要急着自研代理编排，先学会写 Skill，再从现成 Skills 库组装团队。**
> 本目录（`ai-eng-system` + 新增 10 个技能）走的正是这条路——**这个方向被生态验证了**。

**趋势来源**：[GitHub 本周热榜（飙星前五里四个是 Agent Skill）](https://news.qiniu.com/archives/1788747048975)、[2026 年 7 月热门项目盘点](https://juejin.cn/post/7666453566625677339)、[Top 10 Agent Skills Trending](https://shop.zimaspace.com/blogs/tech-ai-hub/top-10-open-source-ai-agent-skills-github)

---

## 2. 规格驱动开发（SDD）—— **最值得你下注的一类**

**核心结论**：代理的失败大多不是"不会写代码"，而是**"没人告诉它要写什么、按什么标准验收"**。SDD 工具就是那个"不存在的产品经理 + 技术负责人"。

**和你的关系**：本仓库的 `to-spec → to-tickets → implement → code-review` 就是这套东西的中文手工版。**对照它们的模板能直接提升你的工单质量。**

| 仓库 | 星标（快照） | 定位 | 对你的具体价值 |
|---|---|---|---|
| **github/spec-kit** | **131.9k** | GitHub 官方 SDD 工具包：`/specify` → `/plan` → `/tasks` → `/implement` | **官方背书 + 生态最稳**。给代理一套硬约束流程，你只提供意图。对"只有我一个人、没人帮我做需求澄清"是最直接的补位 |
| **Fission-AI/OpenSpec** | **66.8k** | 面向 AI 的 SDD，**特别擅长存量项目（brownfield）**：先对齐现状再改 | 你的产品上线后一定是"改存量"而非"从零写"。它的**变更提案机制**让代理改之前先说清改什么 → 直接减少线上事故 |
| **gsd-build/get-shit-done** | **64.6k** | 元提示 / 上下文工程 / SDD 系统，24 → 75 个 CLI 模块 | 号称**单人视角最强的"一个人干一个团队"系统**，比 spec-kit 更偏实战编排 |
| **hesreallyhim/awesome-claude-code** | **49.6k** | Claude Code 生态最权威资源索引 | 你缺的不是工具，是**一整套已被验证的组合**。这是最短的选型路径 |
| **buildermethods/agent-os** | 未核实（v3.0.0 已发布） | 把**代码库标准注入**代理 | 解决最致命的隐性债务：代理反复违反你的约定（命名/目录/错误处理） |
| **automazeio/ccpm** | 未核实 | **用 GitHub Issues + Git worktrees 并行跑多个代理** | **这是"并行"的关键**：多代理各占一个 worktree 各做一张 issue，不会互相踩文件 |
| **tesslio / Tessl** | 未核实 | SDD 商业+开源混合 | 参考其"规格即资产"理念；**商业模式与开源边界未能核实** |
| **jaychempan/Agent-Leaderboard** 等 | 未核实 | 每日更新的生态排行榜 | **保持自己不被时代甩下的仪表盘**（同类：`linny006/trending-claude-skills` 每 15 分钟刷新） |

**可直接取用的技能包**：`vercel-labs/agent-skills` **29.9k**、`vercel-labs/skills` **30.3k**、`google-labs-code/stitch-skills`（+13/天）。

**怎么用**：**不要整套引入**。挑它的**文件模板**（spec 结构、任务分解粒度），对到你自己的 `.scratch/<feature>/spec.md` 与工单里。

---

## 3. 多 agent 编排框架 —— **除非做产品，否则你不需要**

**先说结论**：**多 agent 的难点不在框架，在上下文与交接纪律**（本仓库 `multi-agent-squad` 的核心命题）。
如果你只是想让 DSH 里的 agent 团队干活，用**原生的子 agent / 工作流机制 + 纪律**就够了。

**什么时候才需要下面这些**：你要把 **agent 编排本身做成产品功能**。

| 仓库 | 星标（快照） | 定位 | 什么时候值得用 |
|---|---|---|---|
| **langchain-ai/langgraph** | **41.1k**（+69/天） | 有状态图式代理运行时，可持久化/恢复/人工介入 | **要"生产级可靠性"时**：检查点/中断/恢复是有状态工作流做产品的刚需（用户点了一半、任务失败要续跑） |
| **crewAIInc/crewAI** | **57.2k** | 角色化"代理团队"（role/goal/backstory） | 上手最快（一小时搭出"研究员+写手+审稿"）。适合**内容/运营自动化**，不适合高可靠后端 |
| **agno-agi/agno** | **41.9k**（+21/天，加速） | 全栈代理框架，v3.0 针对"生产里真正会坏的地方" | 定位就是生产，自带记忆/知识/工具/监控，少造轮子 |
| **openai/openai-agents-python** | **29.2k**（+27/天） | OpenAI 官方轻量多代理（handoffs/guardrails/tracing） | 你已有明确流程、不需要大框架时的"够用且不会烂尾"选择 |
| **google/adk-python** | **21.3k** | Google code-first ADK（含 Java 版） | 产品要**嵌进企业客户环境**、给企业交付时的采购筹码 |
| **microsoft/agent-framework** | **13.3k** | AutoGen + Semantic Kernel 合并后的**唯一官方继任者**（Python/.NET） | 要给**企业客户**交付（.NET 环境）时的正统路线。星少是因为太新 |
| **OpenHands/OpenHands** | **84.8k** | 老牌开源自主软件工程代理（原 OpenDevin）+ 可嵌入的 SDK | ①当"第二工程师"跑 issue；②**把 SDK 嵌进你的 SaaS 当卖点** |
| **bytedance/deer-flow** | **81.0k** | 长时程 SuperAgent harness（沙箱/记忆/工具/技能/子代理/消息网关） | **长任务编排的参考实现**：你要"让代理跑几小时把功能做完"，它就是现成架构样板 |
| **openclaw/openclaw** | **387.5k** | 跨平台计算机使用型超级代理 | ⚠️ **生态巨大 = 安全面巨大**。采用前务必读其安全说明（[GitHub Blog 官方访谈](https://github.blog/open-source/maintainers/openclaw-went-viral-meet-the-maintainers-building-and-securing-it/)），**必须沙箱隔离** |
| **daytonaio/daytona** | **71.9k** | 代理/开发环境沙箱（快照、秒级启动） | **代理跑代码必须沙箱**。自己写沙箱是最容易出事又最费时的部分 |
| **e2b-dev/E2B** | **13.7k**（+15/天） | 云沙箱 SDK | "让用户在你的 SaaS 里跑代码"这类产品形态 |
| **livekit/agents** | **13.9k** | 实时语音/多模态代理 | 产品有**语音交互**（客服/教练）时最成熟的实时栈 |
| **VoltAgent/awesome-claude-code-subagents** | **24.6k** | 现成的子代理角色库 | **抄它比自己设计角色快得多**（对照 `multi-agent-squad` 的角色表） |
| **microsoft/ai-agents-for-beginners** | **73.6k** | 微软官方教程 | 补代理概念时性价比最高 |

---

## 4. 全栈脚手架 / 后端 —— 买，不要自己造

**核心结论**：2026 年最实用的变化是——**脚手架开始内置 `AGENTS.md` / skills / agent 规则**。
**模板不只给你代码，还给你"代理该怎么在这个代码库里干活"。选模板优先选这类——它比多一个 UI 组件值钱得多。**

| 仓库 | 星标（快照） | 提供了什么 | 为什么对一个人重要 |
|---|---|---|---|
| **wasp-lang/open-saas** | 未核实（母项目 `wasp-lang/wasp` **18.4k**） | 免费 JS SaaS 模板：React + Node + Prisma；**鉴权（邮箱/Google/GitHub/Slack/MS）、邮件、后台任务、落地页、支付（Stripe + Polar.sh）、Shadcn UI、S3 上传**；**AI-ready：内置定制 `AGENTS.md`、skills、Claude Code 插件**；一条命令部署 | **"直接能卖钱"的最短路径**。鉴权+支付+邮件自写要 2–4 周，它已做完；AI-ready 意味着代理一进仓库就知道规矩 |
| **fastapi/full-stack-fastapi-template** | **45.4k** | FastAPI 官方全栈模板：后端+前端+Postgres+Docker+JWT+CI | **Python 阵营的事实标准**，作者团队维护，不会烂尾 |
| **supabase/supabase** | **108.5k** | 开源 Firebase 替代：Postgres + 鉴权 + 存储 + 实时 + 边缘函数 | **最大时间杠杆之一**：免写鉴权/OAuth/RLS。**建在 Postgres 标准协议上 → 锁定风险低，随时能迁走** |
| **appwrite/appwrite** | **57.0k** | 开源自托管 BaaS | Supabase 的替代；要给**客户私有部署**（数据不出内网）时是加分项。但**自有 API 抽象，迁出成本更高** |
| **pocketbase/pocketbase** | **60.2k** | 单文件后端（SQLite + 鉴权 + 实时 + 后台 UI） | **极端成本优势**：一个二进制就是后端，$5 VPS 能跑。做 MVP 验证阶段最优；SQLite 并发写是天花板 |
| **t3-oss/create-t3-app** | **29.1k** | 类型安全全栈脚手架（Next.js + tRPC + Prisma） | **类型安全 = 代理改错代码时编译器当场抓住**，天然的质检层 |
| **vercel/ai** | **26.0k** | AI SDK：统一多模型调用、流式、工具调用 | 产品里的 AI 功能几乎必然用到；把"接 5 个模型厂商"从几周压到几小时 |
| **shadcn-ui/ui** | **122.2k** | 复制式组件库（代码进你的仓库，不是依赖） | **对代理极其友好**：组件源码在仓库里，代理能读懂也能改 |
| **midday-ai/midday** | **14.7k** | 开源商业产品本体（发票/财务 SaaS），Monorepo 完整 | **不只是模板，是一个真实在跑的 SaaS 的全部代码**（多租户、Stripe、后台任务）。读它比读教程快 |
| **dyad-sh/dyad** | **21.3k** | 本地、免费、开源 AI 应用构建器（"本地版 Lovable"） | **零订阅成本**获得 vibe coding 体验，代码留本地、可上自己的 Git |
| **juspay/hyperswitch** | **43.4k** | 开源支付编排平台（多网关、路由、重试、对账） | 多网关容灾 = 某网关挂了收入不断。**注意：它编排支付，不替代合规主体** |

**选型口诀**：
```
要"能迁走"            → 优先 Postgres 之上的方案（Supabase）
要"最快开始收钱"      → wasp-lang/open-saas（含鉴权+支付+AGENTS.md）
要"零运维、最少钱"    → PocketBase（单文件）
Python 阵营           → fastapi/full-stack-fastapi-template
要"给客户私有部署"    → Appwrite / Coolify + 自己的栈
不要                  → 自己写认证、自己搭数据库高可用
```

---

## 5. 生产就绪 / 质量闸门 —— **你的"替代团队成员"**

**核心结论**：单人 + AI 代理最大的商业风险是**把 AI 写的垃圾代码发上线**（安全事故、数据丢失、客户退款）。
这一节的工具不是锦上添花，而是**你的替代团队成员**：Kodus 替代 reviewer、Semgrep/Trivy 替代安全工程师、Playwright 替代 QA、promptfoo 替代 AI 质量负责人。

**对应本仓库的 `production-readiness`——这些正是把那份清单自动化的手段。**

| 仓库 | 星标（快照） | 定位 | 对你的具体价值 |
|---|---|---|---|
| **microsoft/playwright** | **94.5k** | 跨浏览器端到端测试 | **唯一能证明"用户真能走通付款流程"的手段**。发布前跑一次，比任何单测都值钱 |
| **microsoft/playwright-mcp** | **36.8k**（+30/天） | 让代理**用浏览器自己验证自己写的页面** | **"代理闭环"从能用变可信的分界线**。写完前端自己点一遍、截图确认。**强烈建议今天就接上** |
| **kodustech/kodus-ai** | 未核实（有独立评测专文） | **开源 AI 代码评审代理**，可自托管、可自选模型 | **本类别最值得试的一个**：补上"没人帮我 review"这个最大缺口；自托管+自选模型 = 成本可控。**对照 `code-review` 的双轴设计** |
| **qodo-ai/pr-agent** | 未核实 | 自动 PR 描述、评审、改进建议的 CI 机器人 | 让每个 PR 自动得到第一轮 review，你只看结论。与 Kodus 二选一先跑起来 |
| **semgrep/semgrep** | **16.5k** | 轻量静态分析（SAST），规则可自定义 | 代理最容易犯**注入、鉴权漏判、硬编码密钥**。CI 里几十秒挡住最贵的错误 |
| **aquasecurity/trivy** | **37.4k** | 一体化扫描：依赖漏洞 + 容器镜像 + IaC + 密钥 | **一个命令覆盖全栈安全面**（依赖+Docker+云配置），不该装四五个扫描器 |
| **gitleaks/gitleaks** | **27.7k** | 密钥泄露检测（Git 历史 + 提交时） | 代理很爱把 key 写进代码。**一次泄露可能赔掉整个产品** |
| **trufflesecurity/trufflehog** | **27.7k** | 深度凭证扫描（带**有效性验证**） | 与 gitleaks 互补：它会真去试密钥是否还有效，帮你排优先级 |
| **github/codeql** | **10.0k** | GitHub 官方语义代码分析 | **免费在 GitHub Actions 上跑**（公开库免费），**零运维** |
| **renovatebot/renovate** | **22.4k** | 自动依赖升级 PR | 代理写的代码依赖常过时；自动化修补**可持续**，不靠你记得 |
| **promptfoo/promptfoo** | **24.5k** | LLM 提示词/模型评测与红队（CI 可跑） | 产品含 AI 功能时，**改一次 prompt 就可能全线退化**。它给你"改动前 vs 改动后"的回归测试 |
| **confident-ai/deepeval** | **18.1k** | LLM 输出质量评估（pytest 风格） | 把"AI 答得好不好"变成可断言的测试 |
| **openai/evals** | **19.0k** | OpenAI 官方评测框架 | 生态最大、示例最多，适合起步 |
| **EleutherAI/lm-evaluation-harness** | **13.6k** | 学术级模型评测标准 | 要**自选/自训模型**控制毛利时做横向对比 |
| **grafana/k6** | **31.1k** | 负载测试 | 上线前知道"100 人同时付款会不会挂"，避免最尴尬的失败 |
| **getsentry/sentry** | **44.7k** | 错误追踪与性能监控（可自托管） | **上线后你必须知道客户那边报什么错**。自托管免费，单人可观测入口首选 |
| **vitest-dev/vitest** | **16.7k** | Vite 原生测试框架 | 代理写测试的速度决定你重构的胆量。快 = 你会真的跑 |
| **biomejs/biome** | **25.7k** | Rust 写的 Lint + 格式化 | 快到可以放进代理的**每次编辑循环**，生成瞬间就规范化 |
| **astral-sh/ruff** | **48.8k** | 极快的 Python linter/formatter | 同上，Python 阵营默认答案 |

> **铁规**：**能自动化的闸门，就不要靠纪律。** 人（和 agent）赶进度时一定会跳过检查——让工具替你说"不"。
> 这也正是 `code-review` 要求"两轴并行子 agent"的同一个道理：**把纪律外化成机制**。

---

## 6. 部署 / 运维 / 成本控制

**核心结论**：单人开发者应选 **自托管 PaaS + 托管数据库** 的组合：自托管管应用（省钱可控），托管管数据（安全免运维）。
**同时把 LLM 网关当基础设施装**——它是你 AI 成本与稳定性的唯一闸门。

| 仓库 | 星标（快照） | 定位 | 对你的具体价值 |
|---|---|---|---|
| **BerriAI/litellm** | **57.7k**（镜像亦见 49.9k，存不一致） | 统一 100+ 模型的 OpenAI 兼容网关：路由、重试、**预算/限流**、成本追踪 | **本类最重要**：所有模型调用走它 → 一键换模型、设硬预算、看每用户 token 成本。**直接决定你的毛利率**（对应 `production-readiness` 的"C 成本与滥用"） |
| **coollabsio/coolify** | **60.6k** | 自托管 PaaS（Heroku/Vercel 替代）：Git 推送即部署、数据库、SSL、备份 | **$5–20/月的 VPS 跑完全部服务**，可能省 90% 基础设施费 |
| **Dokploy/dokploy** | **37.1k** | 更轻量的自托管 PaaS | Coolify 的主要竞争者，界面更现代。**二选一，别都用** |
| **basecamp/kamal** | **14.6k** | 零停机部署到**任意 Linux 服务器**（无需 K8s） | 不想装面板就用它：纯 CLI、可放进代理工作流。37signals 出品 |
| **hashicorp/terraform** | **49.2k** | IaC 事实标准 | **让代理管理云资源的正确做法**：基础设施成为可 review、可回滚的代码 |
| **pulumi/pulumi** | **25.4k** | 用真编程语言写 IaC（TS/Python/Go） | **对 AI 代理更友好**：类型检查和 IDE 补全能抓出代理写错的云配置 |
| **Infisical/infisical** | **29.1k** | 密钥管理（替代 Vault：注入、轮换、审计） | 代理时代的关键防线：**密钥不进代码、能轮换、有审计**（对应加固清单 A4） |
| **langfuse/langfuse** | **33.2k** | LLM 可观测与评测（trace、成本、评分，可自托管） | **AI 功能的"生产必备"**：出问题时能看到是哪一步、哪个 prompt、花了多少钱 |
| **Portkey-AI/gateway** | **12.7k** | 高性能 AI 网关（路由、缓存、回退、guardrails） | LiteLLM 替代，更强调性能；流量上量后考虑 |
| **SigNoz/signoz** | **32.0k** | 开源 APM + 日志 + 指标（OpenTelemetry 原生） | **一个自托管后端吃掉全部可观测**，无需 Datadog 月费 |
| **openobserve/openobserve** | **21.6k** | 低成本日志/指标/追踪存储 | 日志成本失控是 SaaS 常见死法；它专门压低存储成本 |
| **hyperdxio/hyperdx** | **9.8k** | 会话回放 + 日志 + 追踪一体 | 排查"用户说点了没反应"时，**会话回放是最快的定位手段** |
| **grafana/grafana** | **76.4k** | 可视化与看板标准 | 把 Coolify + Postgres + LiteLLM + 业务指标汇总到一块屏幕 |
| **dagger/dagger** | **16.2k** | 可编程 CI/CD（本地与 CI 同一套） | **让代理本地跑与 CI 完全一致的流水线**，消灭"在我机器上能跑" |
| **docker/compose** | **37.9k** | 多容器编排 | 单人开发者的默认部署单元就是 compose |
| **Arize-ai/phoenix** | **11.3k** | LLM/RAG 追踪与评测 | Langfuse 的轻量替代，Notebook 友好 |
| **Helicone/helicone** | 快照失败（增速 +2.7/天，偏低） | LLM 可观测与网关 | 曾流行，**增速已明显放缓**；优先 Langfuse / LiteLLM |

**部署判据只有一句**：
```
你要的不是"最强的平台"，而是【回滚 < 5 分钟】的平台。
  □ 一条命令能部署吗？      □ 回滚要几步、多久？
  □ 有预览/暂存环境吗？      □ 账单随用量怎么涨（有没有断崖）？
  □ 数据能不能导出、迁走？
```
托管平台（Fly.io 等）仍可用，但**部署类工具星标不具代表性**——按"你会不会用"选，别按星数选。

---

## 7. 上下文 / 记忆工程

**核心结论**：这是**代理质量的最大单一变量**。2026 年主线是 **MCP 标准化 + Skills 标准化**两条线并行。

| 仓库 | 星标（快照） | 定位 | 对你的具体价值 |
|---|---|---|---|
| **upstash/context7** | **60.9k** | 给代理实时注入**最新版**库文档的 MCP 服务 | **直接消灭"代理用旧版 API 写出编译不过的代码"**——最高频的返工源之一 |
| **oraios/serena** | **28.5k** | 基于 LSP 的语义代码检索/编辑 MCP（符号级而非文本级） | 让代理"按符号理解代码库"，**大仓库里改动命中率显著高于全文搜索** |
| **modelcontextprotocol/servers** | **89.9k** | MCP 官方参考服务器集合（文件/Git/数据库/抓取） | **MCP 是 2026 年事实标准**。接上它，代理立刻有"查库、读文件、调 API"的手脚 |
| **punkpeye/awesome-mcp-servers** | **93.0k** | MCP 服务器全景索引 | 你想接什么能力，**大概率已有人写好**。省时间最多的一个仓库 |
| **github/github-mcp-server** | **32.4k**（+22/天） | GitHub 官方 MCP | **"issue → 分支 → PR → review"全流程交给代理**的官方通道，与 spec-kit/ccpm 组合威力最大 |
| **mem0ai/mem0** | **64.4k** | 通用记忆层（跨会话记住用户偏好与事实） | **产品差异化利器**：让"你的 AI 记得每个客户"成为卖点 |
| **getzep/graphiti** | **30.4k**（+34/天） | 时序知识图谱记忆 | 做"随时间演化的事实"（客户合同变更、项目历史）时，图记忆强于纯向量 |
| **topoteretes/cognee** | **30.2k** | 几行代码给代理装"记忆 + 知识图谱" | 上手成本最低的记忆方案之一 |
| **letta-ai/letta** | **24.5k** | 有状态代理服务（原 MemGPT），分层长期记忆 | 做"与用户长期相处"的代理产品（陪伴/教练/助理）时的完整运行时 |
| **chroma-core/chroma** | **29.2k** | 嵌入式向量数据库 | 单机 RAG 的默认答案，开发体验最轻 |
| **modelcontextprotocol/python-sdk** | **24.1k** | MCP 官方 SDK | **把自己的产品能力暴露给任何代理**（2026 年的新分发渠道）时从这里开始 |
| **PatrickJS/awesome-cursorrules** | **40.7k** | Cursor 规则文件集合 | 上下文工程的"配方库"：抄成熟规则比从零写 `AGENTS.md` 快 |
| **cline/cline**（Memory Bank） | **67.2k** | 代理 + 官方 **Memory Bank** 实践 | **即使不用 Cline 也值得读它的 Memory Bank 文档**——"把项目上下文文件化"最成熟的公开方法之一 |

> **本仓库的立场（重要）**：
> **记忆的第一性原理是"写进文件"，不是"换个记忆服务"。**
> `CONTEXT.md` / ADR / 工单 / handoff 文件就是最便宜、最可控、最可审计的记忆层。
> 记忆服务解决的是**检索**问题；它**不解决**"没写下来"的问题。**先把文件纪律做好，再考虑检索。**

---

## 8. 支付与计费 —— 请极度谨慎

搜索会给你一堆"开源 Stripe 替代品"（`zonelessdev/zoneless`、`tpt-solutions/tpt-payments`、`Flowglad` 等）。

**必须泼一盆冷水**：

```
支付不是"技术选型"，是【合规 + 资金 + 法律】问题。

❌ 不要因为"开源、零手续费、自己掌控"就自建或自托管支付
   → 你可能触及支付牌照 / 资金存管 / 反洗钱义务
   → 出事不是丢数据，是法律责任

✅ 一个人做商业项目的正确做法：
   用成熟的支付服务商（它替你扛合规与风控）
   把服务商包在一个接口后面（见 payment-and-billing）→ 保留换服务商的自由
```

**唯一值得认真看的是支付"编排"层**：

| 仓库 | 星标（快照） | 定位 | 正确用法 |
|---|---|---|---|
| **juspay/hyperswitch** | **43.4k** | 开源支付编排平台（多网关、路由、重试、对账） | 用于**多网关容灾与对账**（某网关挂了收入不断）；**它不替代你的合规主体** |

> 这些开源项目**读代码很有价值**（复式记账、对账、幂等怎么设计）；**不要**当成"省手续费"的方案往生产上放——省下的手续费远小于合规成本。
> 对应 `payment-and-billing` 第一条铁规：**绝不自研支付逻辑。**

---

## 9. TOP 10 最高杠杆选择

排序原则：**对"一个人做到商业上线"的直接贡献**，而非单纯星数。

| # | 项目 | 一句话理由 |
|---|---|---|
| 1 | **github/spec-kit**（131.9k） | 补上你最缺的那个人——产品经理/技术负责人；官方维护，选它最不容易踩空 |
| 2 | **wasp-lang/open-saas** | 鉴权+支付+邮件+后台任务+`AGENTS.md`+Claude Code 插件一次给全，**最快能开始收钱**的模板 |
| 3 | **obra/superpowers**（278.6k） | 把"代理写代码要可靠"做成了方法论和技能包，直接治疗单人开发者的核心恐惧 |
| 4 | **microsoft/playwright-mcp** + **playwright** | 让代理用浏览器自证其代码能跑——**"代理闭环"从能用变可信的分界线** |
| 5 | **BerriAI/litellm** | 一行配置换来换模型自由 + 硬预算 + 每用户成本可见，**直接决定毛利率和生存期** |
| 6 | **hesreallyhim/awesome-claude-code**（配合 `vercel-labs/agent-skills`） | 你不缺工具，缺"被验证过的组合"；这是最短的选型路径 |
| 7 | **Fission-AI/OpenSpec**（66.8k） | 存量改造是商业产品的常态，它让代理"先对齐现状再动手"，直接减少线上事故 |
| 8 | **kodustech/kodus-ai** | 自托管的 AI 代码评审 = 一个不看薪水、不下班的资深 reviewer |
| 9 | **coollabsio/coolify**（或 **Dokploy/dokploy**） | $5–20/月 VPS 承载全部服务，基础设施成本从"每月几百"压到"每月几十" |
| 10 | **upstash/context7** + **oraios/serena** | 一个给代理最新文档、一个给符号级代码理解，一起消灭"用旧 API 瞎写"这个最高频返工源 |

### 预算有限时的一日落地顺序

```
先装 5（LiteLLM 网关控成本）
  → 再装 4（Playwright-MCP 让代理自证）
  → 然后接 2（拿到能收钱的骨架）
  → 用 1（spec-kit）把需求流程固定下来
  → 上线前补 8/9（评审 + 便宜部署）
```

---

## 10. ⚠️ 已放弃 / 被取代 / 不建议采用（**重要，先看这节**）

**这里有"星数很高但已经死了"的项目——最容易踩的坑。**

| 项目 | 状态 | 证据与替代建议 |
|---|---|---|
| **openai/swarm**（21.9k） | **已被取代** | 官方 README 自称 **"Educational framework"**（教学框架），已由 **OpenAI Agents SDK**（`openai/openai-agents-python`，29.2k）接替。**不要用于生产**（[迁移指南](https://www.respan.ai/articles/openai-agents-sdk-vs-swarm)） |
| **AntonOsika/gpt-engineer**（**55.1k**） | **已归档** | 仓库已归档（[来源](https://www.promptquorum.com/zh/power-local-llm/gpt-engineer-review)）。**星数很高但已死**。替代：`dyad-sh/dyad`、spec-kit + 你自己的代理 |
| **MetaGPT**（原 DeepWisdom，约 70k） | **早已停止维护** | "70k Star 的 MetaGPT 早已停止维护，但其 SOP 驱动思路仍被借鉴"（[来源](https://aiproducthub.cn/s/38378.html)）。**思想值得学，代码不要依赖** |
| **Microsoft AutoGen**（原 `microsoft/autogen`，60.4k） | **路线被合并，进入迁移期** | 微软已把 AutoGen + Semantic Kernel 合并为 **`microsoft/agent-framework`**（13.3k）。**新项目请直接用 agent-framework** |
| **returntocorp/semgrep**（旧组织名） | **组织已迁移** | 正确地址是 **`semgrep/semgrep`**。旧链接会重定向，但别写进文档/脚本 |
| **kortix-ai/suna** | **组织迁移 / 状态变动** | 有多处组织/结构变动，增速偏低（+3.6/天）。**上线前请确认其归档状态与官方新地址** |
| **Aider-AI/aider**（48.6k） | **未确认，社区有质疑** | 只确认它仍是活跃讨论对象，**未找到确凿的停维护证据**；因无法核实最后提交时间，**请自行确认**。同类新选择：`charmbracelet/crush`、`sst/opencode` |
| **Helicone** | **增速放缓，存疑** | 增速 +2.7/天（明显低于同类）。**建议优先 Langfuse / LiteLLM** |
| **wasp-lang/SaaS-Template-GPT** | **大概率被取代** | 已被功能更全、且**明确 AI-ready** 的 `wasp-lang/open-saas` 取代 |
| **jeremymanning/ccpm** | **疑似 fork / 非原始** | 请用 **`automazeio/ccpm`** |

---

## 11. 我明确无法验证的事项（避免误导）

1. **所有星标数的精确值**——出网被阻断，无法用 GitHub API 核对；第三方快照之间存在冲突（尤其 `litellm`）。
2. **"最后提交时间 / 是否仍在积极维护"**——无法逐仓核实。标注"未核实"的星标中，尤其 **`wasp-lang/open-saas`、`buildermethods/agent-os`、`automazeio/ccpm`、`kodustech/kodus-ai`、`qodo-ai/pr-agent`**，**建议你打开仓库看一眼 commit 频率再决定**。
3. **部分仓库的规范 owner/name**——`stackblitz/bolt.new`、`gotalab/cc-sdd`、若干 SaaS 模板无法确认规范地址。
4. **许可证**——未逐仓核实。**商业产品必须自己确认**（见第 0.1 节第 4 条）。
5. **中文榜单的具体项目名**——搜索结果只返回标题未返回内容，无法列出其中仓库名。建议直接读 [qiniu 周榜](https://news.qiniu.com/archives/1788747048975) 与 [juejin 7 月盘点](https://juejin.cn/post/7666453566625677339)。
6. **`openclaw` 的架构/许可/安全模型细节**——387.5k 星与"全球 #6"经多来源交叉印证较可信，但**采用前务必读其安全说明**。

---

## 12. 诚实结论与建议

### 对你的判断（明确说，不绕）

1. **你当前最大的瓶颈，这些工具帮助有限。**
   你要的是"一个人有完整商业能力"。**瓶颈在商业验证与交付纪律，不在缺框架。**
   所以本目录新增的价值主要在 `commercial-squad` 的四道闸门 + `multi-agent-squad` 的编排纪律，**而不是**引入更多运行时依赖。

2. **生态给你的最大启示**：**2026 年霸榜的是"技能层"而不是"编排框架"**——
   这正说明本目录走的方向（把纪律固化成技能）是对的。**继续写技能，比换框架回报高得多。**

3. **最值得你花时间的三个方向**：
   - **SDD 的模板形状**（spec-kit / OpenSpec）→ 直接改进工单质量
   - **质量闸门自动化**（Playwright-MCP / Semgrep / gitleaks / Kodus 进 CI）→ 直接降低上线风险
   - **卖后端不要自己造**（Supabase / open-saas / PocketBase）→ 直接省掉两个月

4. **明确不建议现在做的**：
   - 引入多 agent **框架**（除非把 agent 编排做成产品）
   - 自建/自托管**支付**
   - 上**记忆服务**（先把文件纪律做到位）
   - 一次性引入多个"运行时型"项目

### 怎么自己复验（比抄清单重要）

```
对每个候选项目，打开 GitHub 页面看四样：
  ① Commits 最近时间（是不是活的）
  ② Releases 最近版本（有没有在发版）
  ③ Open Issues / 已关闭比例（维护者管不管）
  ④ 核心依赖是不是标准协议（能不能迁走）

任何一条不满足 → 降级为"参考资料"，不要进生产。
```

### 如果只想抄一个东西

**抄 spec-kit 的规格与任务模板形状**——因为它和本仓库的 `to-spec`/`to-tickets` 同源，是**投入产出比最高**的一件事：
不需要引入任何运行时，只改进你写工单的方式，而**工单质量直接决定多 agent 团队能不能跑起来**（见 `multi-agent-squad` 坑位 4）。

---

## 附：本节速查

```
四问筛选
  标准协议之上？/ 最近6个月活着？/ 解决你的瓶颈？/ 方法论还是运行时？

2026 最大信号
  霸榜的是【技能层】，不是编排框架 → 继续写技能比换框架回报高

分类速记
  SDD          → 【最值得抄】spec-kit / OpenSpec / get-shit-done
  多agent框架  → 除非做产品，否则不需要（难点在纪律不在框架）
  全栈后端     → open-saas（最快收钱）/ Supabase（能迁走）/ PocketBase（最省）
  质量闸门     → Playwright-MCP + Semgrep + gitleaks + Kodus 【进 CI】
  部署成本     → LiteLLM 网关 + Coolify/Dokploy（$5-20/月 VPS）
  上下文       → context7（最新文档）+ serena（符号级代码理解）
  支付         → 不自研、不自托管；只用编排层（hyperswitch）做容灾
  记忆         → 先做好文件纪律，再谈记忆服务

一日顺序
  LiteLLM → Playwright-MCP → open-saas → spec-kit → Kodus/Coolify

已死名单（别用）
  openai/swarm（教学）· gpt-engineer（已归档）· MetaGPT（停维护）
  microsoft/autogen（已并入 agent-framework）· returntocorp/semgrep（旧地址）

铁律
  方法论型随便抄，运行时型要当结婚对象
  能自动化的闸门就不要靠纪律
  数字会变，自己打开仓库看 Commits / Releases / License
```
