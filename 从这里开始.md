# 从这里开始（一页纸）

> **本文件是唯一入口。** 其余文档都是**按需查阅**，不用一开始读。
> 目标：**读完这一页，你就知道该跑哪条命令。**

---

## 〇、命令前缀（**先看这一条，否则命令会直接报错**）

下面的命令都写成这样：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File <脚本> <参数>
```

**不是啰嗦，是实测**（2026-09-12 在这台机器上验证过）：

| 你可能会写的 | 实际结果 |
|---|---|
| `pwsh -File x.ps1` | ❌ **本机没装 PowerShell 7** → `The term 'pwsh' is not recognized` |
| `.\x.ps1` 或 `powershell -File x.ps1` | ❌ **执行策略是 `Restricted`** → `running scripts is disabled on this system` |
| `powershell -NoProfile -ExecutionPolicy Bypass -File x.ps1` | ✅ 可跑 |

- `-ExecutionPolicy Bypass` **只对这一条命令生效**，不改你机器的全局设置（想一次性改掉是你自己的决定：`Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`）。
- `-NoProfile` 是为了不受你 profile 里的副作用影响。
- `.mjs` / `node` 命令不受影响，照抄即可。
- 装了 PowerShell 7 的话，把 `powershell` 换成 `pwsh`、并保留 `-ExecutionPolicy Bypass` 即可。

---

## 一、你是哪种情况？

### 情况 A · 我要开始一个**新项目**（或新功能）

```powershell
# 1) 把技能装到 DSH（只需一次）
powershell -NoProfile -ExecutionPolicy Bypass -File <库根>\install-skills.ps1

# 2) 在你的项目里建中央状态
powershell -NoProfile -ExecutionPolicy Bypass -File <库根>\scripts\state.ps1 init -Project .

# 3) 开始对齐意图：写出三张图 + 接口契约（模板见下）
#    模板：<库根>\engineering\multi-agent-squad\CONTEXT-TEMPLATE.md
#    → 产出 CONTEXT.md

# 4) ⛔ 硬结构闸门：没过不许写代码
node <库根>\scripts\verify-structure.mjs -Project .
#    退出码 1 = 停下，回去补图
```

过了第 4 步，**才**开始写代码。然后按 `to-tickets` 的工单一张张做。

> **三个冷启动实测暴露的坑**（第一次跑这套流程的人都会撞上）：
>
> 1. **"开工"是哪条命令？** —— `state.ps1 init` 后停在 `phase=idea`，
>    写完三张图是 `tech-review`（**收尾对齐，还没开工**）。**正式开工 = `phase -Value g3-build`**：
>    ```
>    powershell -NoProfile -ExecutionPolicy Bypass -File <库根>\scripts\state.ps1 phase -Value g3-build -Note "三图+契约已过闸门"
>    ```
>    （商业档要先过 `g1-validate`/`g2-price`，见情况 C。）
> 2. **模板里除了三张图，第四节还有"四条踩过的坑"** ——
>    那段比图更重要：它讲清了校验器怎么切"端点块"、什么写法会假警报。
>    **只看图的范例就开写，很可能在闸门上来回试错。**
> 3. **别去读校验器的源码来"对答案"** —— 读源码能让闸门一次过，
>    但那等于绕过闸门（冷启动里真发生过，agent 自己如实报告了）。
>    卡住了就回来看模板第四节，或者问用户。

---

### 情况 B · 我有个东西要**上线/给别人用**

```powershell
# 自用 / 局域网演示 → 只跑红线（秒级）
powershell -NoProfile -ExecutionPolicy Bypass -File <库根>\scripts\redline-check.ps1 -Project .

# 给真实外部用户 / 要收费 → 加重装闸门
powershell -NoProfile -ExecutionPolicy Bypass -File <库根>\scripts\quality-gate.ps1 -Path . -IncludeTests -Json gate-report.json
#    然后逐项过 production-readiness 的 A–G
```

**红线 4 项**：硬编码密钥 · 无保护 API · 违法数据源 · 分发他人内容
**后两项机器查不了**，要人答一句：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File <库根>\scripts\state.ps1 redline -Name data_source_ok  -Note "为什么合法"
powershell -NoProfile -ExecutionPolicy Bypass -File <库根>\scripts\state.ps1 redline -Name no_redistribute -Note "依据"
```

> **五条必须知道的语义**（冷启动实测暴露；第 2、3 条是 2026-09-13 三项目实测与独立复审后补的）：
>
> 1. **结论行有三档，三档**都**不等于"红线已过"**：
>    `PASS` / `PASS（带警告）` / **`PASS（有 N 项没跑成）`**。机器只回答"有没有硬伤"；
>    **③④ 没人工确认、`tech_redline` 闸门没标通过，就等于没过**——别把 exit 0 读成"可以外发"。
>    ⚠️ 最后一档最容易被误读：它表示**有整项没查**，而 ①②③ **任何一项**"一个可扫文件都没有"
>    都会进这一档（① 没有可扫文件、②③ 没有 `.py/.js/.mjs/.cjs/.ts/.tsx/.jsx/.vue`），
>    **那几项没有结论**，不能算"检查过、没问题"。这一档在 `-Quiet` 下也照打。
>    （2026-09-13 三项目实测暴露的 N1：旧版在非 git 项目上 ① 整项没跑，顶层照样打 `PASS（带警告）`；
>    第一轮修复只堵了 ①，②③ 的同类情况在第三轮复审才被照出来。）
> 2. **① 分两档，而且"未发现"带范围，别读成全称否定。**
>    · **被跟踪**的文件里命中密钥 ⇒ **硬失败**（它已经/将要进仓库，没得商量）；
>    · **未被跟踪 / 被忽略**的文件（`.gitignore` 里的 `.env` 就是这类）**也会扫**，
>      但命中只记**警告**——那类文件本来就该放密钥，"是不是问题"取决于这个目录会不会
>      被打包/同步/上传，**由你判断**（2026-09-13 用户裁定：git 项目也扫，与非 git 一致）。
>    · **不扫**的有三类：依赖/产物目录里、按扩展名判为二进制（含 `.db`/`.sqlite`）、
>      超过 2MB。工具会把**每一类的条数**写在汇总行的范围注里，不静默。
>    · **工具只读当前文件，任何项目都不查 git 历史**——旧版本里出现过的密钥它答不了。
> 3. **不是 git 仓库也能跑**（N1 的另一半，2026-09-13 用户裁定后补的）：
>    ① 会**回退扫描整个工作区**（跳过 `node_modules` / `.venv` / 缓存目录，以及二进制/资源），
>    命中记成**警告**（"需人过一眼"）而不是硬失败——非 git 没有 `.gitignore` 依据，
>    测试夹具、文档示例里的假密钥不该把闸门变成狼来了。
>    但要注意它**扫的是当前文件**：`git ls-files` 那套"被跟踪的敏感文件 / .gitignore 覆盖"
>    仍然只对 git 项目生效。
>    **夹具里放的是假密钥、被误判了？** 在那一行末尾写 `redline-allow`（外加一句理由），
>    该行即被豁免——**豁免条数与文件名:行号都会报出来**（含 `-Quiet` 模式），不会静默；
>    真密钥不许这么标。
>    **同一个标记，重型闸门也认**（2026-09-13 统一）：`.gitleaks.toml` 让 gitleaks 放行带
>    `redline-allow` 的行——以前两套检查器对"这是样例"没有共同语言，实测在库自己身上误报过
>    2 处；semgrep 那边用**同一行**的 `// nosemgrep: <规则 id>`。**一行标记，两个扫描器都放行。**
> 4. **`从红线 exit 0` 也不代表能外发。** 红线是**自用档的及格线**；
>    要给外人用/要收费，必须走上面那条**重装闸门**，两者不是一回事。
> 5. **`-Json` 写出的报告是"某一次运行的快照"，不是状态文件。**
>    它会一直躺在项目根、然后被下一个人（或下一个 agent）当成当前结论——
>    实测踩过：一份写着 `fail=0` 的旧报告 vs 真实重跑 `fail=2`。
>    闸门现在会主动警告旧报告，但**最好的做法是别把它留在项目根**（或写进 `.gitignore` 的临时目录）。

---

### 情况 C · 我**决定要把这个拿去卖**

```powershell
# 先过红线（商业档必须先过它，状态机会强制）
powershell -NoProfile -ExecutionPolicy Bypass -File <库根>\scripts\redline-check.ps1 -Project .
powershell -NoProfile -ExecutionPolicy Bypass -File <库根>\scripts\state.ps1 gate -Name tech_redline -Passed -Evidence "红线通过"

# 切到商业档 → 启动 G1–G4（含期限与退出判据）
powershell -NoProfile -ExecutionPolicy Bypass -File <库根>\scripts\state.ps1 tier  -Value commercial
powershell -NoProfile -ExecutionPolicy Bypass -File <库根>\scripts\state.ps1 phase -Value g1-validate
#    然后读 commercial-squad 的四道闸门
```

> **G1–G4 默认不启动。** 日常自用项目跑红线就够了——**一个没人跑的闸门等于没有闸门**。

---

### 情况 D · 我要**维护这套体系本身**（改技能 / 加技能）

```powershell
cd <库根>

# 最快的一条路：跑完下面全部检查（不依赖 git 钩子；CI 里也用它）
node scripts\verify-all.mjs --installed "<DSH技能根>"

# 一次性：装提交前自检（之后每次 git commit 自动跑下面这些，不用记得）
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\install-git-hooks.ps1

node validate-skills.mjs "<DSH技能根>"            # 技能 frontmatter 体检
node scripts/verify-ps1.mjs -Repo .               # .ps1 的 BOM + 语法（可加 -Fix）
node scripts/extract-cheatsheets.mjs --check      # 速查卡是否与正文漂移
node scripts/check-consistency.mjs --installed "<DSH技能根>"   # 文档数字/清单/链接/命令 ⇄ 真值
node scripts/self-test.mjs                        # 闸门自己有没有坏（改了检查器后必跑）
powershell -NoProfile -ExecutionPolicy Bypass -File install-skills.ps1   # 同步到技能根
```

> **钩子 vs `verify-all` 的分工**：钩子**拦住提交**（本机、可被 `--no-verify` 绕过）；
> `verify-all` 是**不依赖任何本地状态**的入口——换机器、clone 一份、CI 里跑的都是它。
> 两者的检查项**同源**，但**只有 `verify-all` 能在没有钩子的地方跑**。
> （上面这一套一共是**七个检查器**：frontmatter 体检 / `.ps1` BOM 与语法 / **编码与行尾** / 内容一致性 / 速查卡漂移 / 闸门完整性 / 检查器自检。）

**六条纪律**：
1. **改源 → 跑同步**，别直接改技能根里的副本
2. **新增技能必须同步** `README.md` / `SKILLS.md` / `ask-matt`
3. **新增脚本／改文档里的数字** → 跑 `check-consistency.mjs`（它不信文档，只信文件系统）
4. **`edit` / `write` 工具会吃 BOM** → 改完 `.ps1` 必跑 `verify-ps1.mjs -Fix`
5. **加全局约束前先问**：这一步有没有更便宜的检查？
6. **改了 `scripts/` 或 `tests/`（检查器 / 闸门 / 钩子 / 样例）→ 必须起独立上下文的 reviewer 复审**（见下）

> **为什么只管 `scripts/` 和 `tests/`，不管技能正文**（两档，别搞混）：
> **重装档 = 检查器/闸门/钩子** → 改错了会**静默关掉一整类保护**，所以必须独立复审。
> **轻量档 = 技能正文 / 文档** → 已经有机器在跑（frontmatter 体检、内容一致性、速查卡漂移、
> 链接与覆盖率），出错的代价是"一句话写得不好"，不是"闸门失灵"。
> **想要更严可以自愿加审，但不强制** —— 把每一处措辞改动都要求独立复审，
> 结果只会是这条规矩被当成形式、然后被忽略（和 G1–G4 太重的教训一样）。

#### 纪律 6：为什么"自己改、自己判"不算审

**这不是形式主义，是实测**（把三件事的来历分开写，别混成一句口号）：

| 检查器的 bug | 怎么被发现的 |
|---|---|
| 只认不加粗的 `\| 错误 \|`，而模板印的是加粗版 | **独立 agent** 拿真实项目跑，照模板抄卡住了 |
| 把契约段开头的铺垫文字当成一个端点块 | 同上，同一次真实项目跑出来的假警报 |
| 统计自研技能行的正则只匹配到 2/10 行 | **我自己**写的负向测试发现的（不构成独立证据） |

**结论**：两次靠独立 agent、一次靠负向测试 —— **所以这条规矩不是凭空的**，
但也**别夸大成"凡是自查都不可靠"**：真正可靠的结论是
「**改的人看不出自己的错，需要另一双眼睛或一个会失败的测试**」，
而**测试只有在你想到该测什么的时候才有用**（第三条 bug 就是靠"造一份必错的样例"才现形的）。

同一条铁律（「同一个 agent 不能既实现又审查自己」）对**库本身**同样成立，而且更容易被违反：
检查器是我写的、检查什么是我定的、判绿也是我判的。

> ⚠️ **自检证明不了这件事**：`self-test.mjs` 只能证明"**判据仍然成立**"，
> 证明不了"**判据选对了**"——用例全绿，也可能整体漏掉了该检的那一类。
> 所以这一步**没有机器能替你验**，只能靠这条规矩。**不要伪造它**：
> 假装的审查比没有审查更糟，因为它会让"没人看"看起来像"有人看过"。
>
> **留痕（推荐，不强制）**：把复审结论落到 `docs/reviews/<日期>-<基点sha>.md`。
> 不强制是因为伪造一个空文件毫无成本，强制只会制造"看起来审过"的假象；
> 但**真做过就留下**，下一次复审时那份记录就是这条纪律唯一的实证来源。

**复制即用的 reviewer 提示词**（把 `<...>` 换掉）：

```
【角色】Reviewer。你看不到改动者的任何推理过程，只按下面的材料独立判断。
【材料】① 改动：在 <库根> 跑 `git diff <基点>..HEAD`
        ② 改动者声称解决的问题：<一句话结论，不给他理由>
        ③ 判据来源：相关 SKILL.md 正文、检查器头部注释里写的"为什么"
【任务】只回答三件事，不要复述改动内容：
        ① 这个改动**真的**解决了它声称的问题吗？给反例或边界情况。
        ② 它**新引入**了什么？（新依赖、新假设、新的"永远为真"判据）
        ③ 哪些地方**本该被检查，却没有任何检查覆盖**？
【纪律】不许改代码；每条发现写成「证据（文件:行）→ 后果 → 建议」。
```

**它抓什么，不抓什么**（说清楚，免得当成万能）：

| 能抓 | 抓不了 |
|---|---|
| 声称解决了 A，但实际只覆盖 A 的一半 | 你的**意图**对不对（那要问你） |
| 新判据永远为真 / 永远为假（等于没判） | 你没意识到的需求 |
| 改动引入的新假设、新依赖 | 判断"这次改动值不值得做" |


> **钩子已装**（`core.hooksPath = scripts/git-hooks`）：上面这些会在 `git commit` 时自动跑，不过就不让提交。
> 绕过 = `git commit --no-verify`：**那次提交既没有任何检查，也没有复审提醒**（提醒也挂在钩子上），
> 而且**不留痕**——事后没人能区分"审过"和"没审"。别当常规操作。

---

## 二、我该用哪个技能？（**不要背 36 个名字**）

| 你的处境 | 去问 |
|---|---|
| 工程流程问题（"该用哪个技能？"） | **`ask-matt`**（路由器） |
| 商业流程问题（"我走到哪一关了？"） | **`commercial-squad`**（路由器） |

**其余技能不用记。** DSH 会在每个新会话里**自动注入技能目录**（名字 + 一句话），
需要时 agent 自己会加载。**你只要知道这两个路由器就行。**

---

## 三、十九个脚本，一句话各自干什么

| 脚本 | 干什么 | 什么时候跑 |
|---|---|---|
| `verify-all.mjs` | **一条命令跑完下面全部检查**（不依赖 git 钩子；CI 也用它） | 换机器 / CI / 想一次性确认 |
| `install-skills.ps1` | 把技能打平安装到 DSH 技能根 | 装/更新技能后 |
| `validate-skills.mjs` | 技能 frontmatter 体检（YAML / 描述长度 / 嵌套） | 改了技能后 |
| `verify-ps1.mjs` | `.ps1` 的 **BOM** + 语法（`-Fix` 自动补 BOM） | 改了 `.ps1` 后 |
| `verify-encoding.mjs` | **编码与行尾**体检：`.ps1` 要 BOM、`.bat/.cmd` 要 **GBK+CRLF**、其余文本要 UTF-8 **无 BOM**；还会抓"已被写坏"的 `U+FFFD` 替换字符 | 改了 `.bat` / 遇到乱码 / 提交前（已进 `verify-all`） |
| `verify-structure.mjs` | **硬结构闸门**（三图 + 契约） | **进 Builder 前** |
| `state.ps1` | 中央状态（`init`/`show`/`validate`/`phase`/`gate`/`artifact`/`redline`） | 切阶段、记闸门 |
| `redline-check.ps1` | **技术红线**（4 项，秒级） | 日常 / 上线前 |
| `quality-gate.ps1` | 加固全审（15 项；不测则 14 项） | 给真人用前 |
| `check-consistency.mjs` | **内容一致性**（文档里的数字/清单/链接是否与真值矛盾） | 改了文档或加了脚本后 |
| `install-git-hooks.ps1` | 装/卸**提交前自检钩子**（`core.hooksPath`，跟代码走） | 只需一次 |
| `self-test.mjs` | **检查器自检**（变异测试：注入已知错误，断言闸门必须拦住） | 改了任何检查器后 |
| `extract-cheatsheets.mjs` | 速查抽取 + **漂移检测**（`--check`） | 改技能正文后 |
| `state.schema.json` | 状态文件的 schema（`state.ps1 validate` **真的会读它**：类型 / 深层必填 / 枚举） | 参考 |
| `facts.json` | **事实登记表**（关于体系自身的数字；新增体系数字必须登记在这） | 改了文档里的体系数字后 |
| `verify-clean-clone.mjs` | **干净房间验证**：clone 到临时目录 + 刮干净环境跑全套（= CI 的核心收益，零外泄） | 想确认「换台机器还能不能跑」时 |
| `verify-gate-integrity.mjs` | **闸门完整性**：期望的检查项**钉在闸门外面**比对（闸门内部那份清单和"真跑出来"是同一侧的，删一项检查它俩会一起变小） | 改了 `quality-gate.ps1` 后 |
| `ruff-label.ps1` | ruff 规则集标签（**单独一份**：闸门的 `-RuffLabel` 早退要用它，`self-test` 也靠它自动化那七种情形） | 改 ruff 相关逻辑时 |
| `quality-gate.workflow.yml` | CI 模板（给**项目**用的） | 复制进项目 |
| `BEHAVIOR-VERIFICATION.md` | `tests/fixtures/` 里：**行为验证协议**（schema 校验 / ruff 配置发现怎么验、判据先写死；静态用例证明不了行为） | 改了 `state.ps1` 或 quality-gate 后 |

---

## 四、两个"别做"（最容易踩的过度设计）

**① 别按前端/后端拆 agent。**
> "前端"/"后端"是**产物**的分区，不是 **agent** 的分区。
> **默认：一张工单一个 Builder，他做完那一条垂直切片。**
> 只在"两边都不需要读对方代码 + 工单真互不阻塞 + 验证方式完全不同"时才拆。
> 详见 `fullstack-delivery` 第 7.0 节。

**② 别把闸门当优秀线。**
> **红线是及格线**，只回答"有没有踩线"。"有没有人要、能不能收钱"是另一回事（G1/G2）。

---

## 四点五、**用户不在线时怎么办**（铁律 2 的降级规则）

**问题**：铁律说"含糊就先问用户、不许猜"，但用户常常不在线（发个需求就走）。
**这两条会直接冲突**，而文档以前没有降级规则——冷启动测试里 agent 只能自己发明一套，还如实报告了它的不安。

**规则**（三档，按危险程度分）：

| 情况 | 怎么办 |
|---|---|
| **事实类问题**（"这库用什么框架""有几个端点"） | **自己查**，不许问用户（查不到就标明"查不到"） |
| **决策类问题**（选型、定价、范围、开哪些功能） | **可以带假设往前走，但必须同时满足三条**：① 每条假设**落文件**（交接文件/`CONTEXT.md` 的"待确认"一节）② 假设**成本可控**（改起来便宜：不改数据结构、不发布、不花钱）③ **不许越过闸门**——到了需要用户拍板的那道门就停 |
| **不可逆 / 花钱 / 对外动作**（发布、删数据、改 schema、接入付费服务） | **一律停下**，把问题清单交出来等回复。**猜测在这里没有豁免** |

**为什么允许"带假设往前走"**：完全停摆的代价是"用户回来时什么也没推进"，
而**留下可追溯的假设清单**让用户 30 秒就能裁定——比空等便宜。
但**"往前走"只到闸门为止**：闸门就是设计给"用户不在场时"用的护栏。

> ⚠️ **这一节只降级铁律 2（不许猜意图），不改变铁律 1（改代码/配置仍须用户确认）。**
> 具体说：**带假设往前走 ≠ 可以动手改**。第二节那份假设清单是**给用户裁定的输入**，
> 不是"先改了再说"的许可；**动代码/配置、发布、花钱、删数据**这几件事，
> 无论假设多合理，都要等用户点头（第三档）。
> 判据（照着念）：**假设能不能写进 `CONTEXT.md` 的「待确认」一节？能写 ≠ 能做。**

---

## 五、接下来读什么（**按需，别一次读完**）

| 我想… | 读 |
|---|---|
| 搞清体系全貌 | `README.md` |
| **我不写代码，只想把想法做成生意** | **[`docs/给非技术用户的使用指南.md`](./docs/给非技术用户的使用指南.md)**（全程人话，不用敲命令） |
| **想知道它到底灵不灵（一页结论）** | [`docs/验证报告.md`](./docs/验证报告.md)（测了什么 · 照出什么 · 哪里还不确定） |
| 知道 DSH 的坑（编码 / 调用策略 / 技能发现） | `DSH-INTEGRATION.md` |
| 搭多 agent 团队 | `engineering/multi-agent-squad/SKILL.md` + `多Agent团队协作编排指南.md`（完整走查版） |
| 一个人怎么把商业项目做完 | `commercial-squad` → `product-validation` → … |
| 引入第三方库前 | `开源技术引用.md`（含**选型四问**） |
| 查某个具体机制 | `速查卡.md` · `术语对照表.md` |
| 许可与致谢 | `THIRD-PARTY-NOTICES.md` |
| 别踩"星高但已死"的坑 | `开源生态参考.md` |

---

**记住三条**：
1. **先对齐意图（三张图），再写代码。**
2. **闸门要跑才算数**——`state.ps1 validate` 会说"你现在的状态自不自洽"。
3. **两个路由器够用了**：工程问 `ask-matt`，商业问 `commercial-squad`。
